{
  "name": "@runback/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "계정·저장 플랜·수행 기록 저장소. Prisma + Postgres",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "seed": "node --env-file=../../.env src/seed.ts",
    "user:create": "node --env-file=../../.env src/cli-user.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^6.16.0"
  }
}
