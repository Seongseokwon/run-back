node_modules/
dist/
.next/
.next-agent/
out/
coverage/
*.tsbuildinfo

# Prisma 생성 산출물. prisma generate 로 언제든 다시 만든다
packages/db/generated/
.env
.env.*
!.env.example
.DS_Store

# Claude Code 스킬 — 프로젝트가 아니라 사용자(~/.claude/skills)에 둔다.
# 여기 두면 이 리포에서만 쓸 수 있는데, 범용 디자인 스킬이라 그럴 이유가 없다
.claude/
