# RUNBACK — 대회 역산 러닝 훈련 플랜 서비스 PRD (MVP v1.3)

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v1.3** (2026-09-10) |
| 개정 이력 | **v1.3** — 도메인 `runback.kr` 확정(O1' 종결). 백엔드 스택 확정: Postgres(Docker/Neon) + Prisma. 애플 로그인 1차 제외(O12 종결), 탈퇴 유예 15일 확정(O11 종결). `SessionLog` 키를 날짜로 변경. 국외 이전 고지 조항 추가<br>**v1.2** — 제품명 RUNBACK 확정. 카카오 비즈앱 심사가 임계 경로가 **아님**을 조사로 확인해 §9.3·§16·§17 개정. O1·O2·O3 종결, O8·O9 해소<br>v1.1 — 회원가입/로그인을 Non-goal에서 P0로 승격, §9 신설. 릴리즈 6주 → 8주 |
| 상태 | Draft — 1차 릴리즈 범위 확정용 |
| 제품명 | **RUNBACK** (2026-09-08 확정) · 도메인 **runback.kr** (2026-09-10 확정) |
| 범위 | MVP 1차 릴리즈에 집중. 2차 이후는 §16에 한 페이지 요약만 |
| 플랜 생성 방식 | 규칙 기반 엔진 + LLM 보조 레이어 (역할 경계는 §7.9) |
| 대회 데이터 | 수동 큐레이션으로 시작 (§12) |
| 작성 근거 | 시장·경쟁 조사 결과는 §2, 훈련 방법론 출처는 §7 각 절 |

---

## 1. 한 줄 정의

> **"내가 나가는 그 대회 날짜"를 기준으로 역산해서, 지금 실력과 목표 기록에 맞는 주차별 훈련 플랜을 만들어 주는 모바일 웹.**

기존 서비스와의 차이는 훈련법 자체가 아니라 **앵커**다. 경쟁 서비스는 "하프 12주 플랜"을 주고, 우리는 "11월 15일 MBN 서울 마라톤까지 남은 10주, 목표 1:55 기준 플랜"을 준다.

---

## 2. 배경 및 문제 정의

### 2.1 시장 상황

- 국내 마라톤 대회 참가자: 2020년 약 9천 명 → 2024년 **100만 명**(254개 대회). 5년 만에 100배 이상.
- 조깅·달리기 참여율: 2024년 4.8% → 2025년 7.7% (문체부).
- 업계 전망: 3~5년 내 대회 참가 인구 150만~200만 명.

시장은 구조적으로 성장 중이며, 대회 참가자가 폭증한다는 것은 **"대회 날짜"라는 앵커를 가진 사용자가 폭증한다**는 뜻이다. 이 PRD의 전제.

### 2.2 경쟁 공백

| 구분 | 대표 서비스 | 훈련 플랜 | 국내 대회 연동 | 개인화 |
|---|---|---|---|---|
| 글로벌 유료 코칭 | Runna(Strava), TrainAsONE, Athletica | O | X | O |
| 글로벌 무료 | Nike Run Club, Garmin Coach | O | X | 부분 |
| 브랜드 콘텐츠 | 나이키 코리아, On | O (정적 한국어) | X | X |
| 플랜 생성기 (SEO 툴) | CreateRunPlan, RunDida, PacePercentile 등 다수 | O | X | 얕음 |
| **국내 러닝 서비스** | **러너블, 고러닝, 아임마라톤, 랭킹마라톤** | **X** | **O** | **X** |

**핵심 발견:** 국내 서비스는 *기록 관리 / 대회 정보 / 커뮤니티*에 전부 몰려 있고 **훈련 처방을 하는 곳이 없다.** 반대로 훈련 처방을 하는 글로벌 서비스는 **국내 대회를 모른다.** 이 교집합이 비어 있다.

### 2.3 우리가 푸는 문제

러너의 실제 언어는 "10km 훈련법 알려줘"가 아니라 **"11월 15일 하프인데 지금부터 뭘 해야 하지?"** 다. 기존 도구는 전자에 답하고, 후자에는 사용자가 직접 주차를 세어 맞춰야 한다.

**해결:** 대회 날짜를 입력받아 D-day를 역산하고, 남은 기간에 물리적으로 가능한 플랜만 제시한다. 기간이 부족하면 **목표를 낮추라고 말해준다.** (이게 정직한 차별점이자 §7.3의 핵심 기능)

---

## 3. 제품 가설과 성공 기준

### 3.1 검증할 핵심 가설

| # | 가설 | 검증 방법 |
|---|---|---|
| H1 | 대회 날짜 앵커가 있으면 플랜 생성 후 재방문한다 | **D+7 재방문율** |
| H2 | 국내 대회명 롱테일 키워드로 유기적 유입이 발생한다 | 대회 페이지 오가닉 세션 |
| H3 | 목표 타당성 피드백(§7.3)이 신뢰를 만든다 | 목표 재조정 후 생성 완료율 |

### 3.2 성공 기준 (릴리즈 후 8주)

| 지표 | 판정선 | 의미 |
|---|---|---|
| **플랜 생성 후 D+7 재방문율** | **≥ 20%** | **North Star.** 미달 시 제품화 중단, 애드 수익형 도구로 확정 |
| 플랜 생성 완료율 (진입 → 결과) | ≥ 60% | 입력 마찰 점검 |
| 오가닉 세션 중 대회 페이지 비중 | ≥ 40% | SEO 전략 유효성 |
| 플랜 저장/공유 발생률 | ≥ 15% | 소유감 형성 여부 |
| 게스트 → 로그인 전환율 | ≥ 25% | 저장 게이트 위치가 적절한지 (§9.2) |
| 로그인 사용자 D+7 재방문율 | 게스트 대비 관측 | H1의 정밀 측정 수단 (§9.9) |

> **판단 규칙:** D+7 재방문율이 20% 미만이면 §16의 2차 로드맵을 실행하지 않는다. 생성기는 그대로 두고 광고/제휴 최적화만 한다.

### 3.3 Non-goals (MVP에서 명시적으로 하지 않는 것)

| 제외 항목 | 이유 |
|---|---|
| 애플 건강 / 삼성 헬스 자동 연동 | **HealthKit은 웹에서 접근 불가.** 백엔드 API 자체가 없고 네이티브 iOS 브리지 앱이 필수. MVP에서 손대면 안 됨 |
| Strava 연동 및 소셜/랭킹 | 2024년 11월 API 약관 변경으로 제3자 앱의 **타 사용자 데이터 표시가 금지**됨. 크루/리더보드 설계 불가 |
| GPS 러닝 트래킹 | NRC·애플워치와 정면 경쟁. 이길 수 없고 웹으로 못 함 |
| 네이티브 앱 | SEO 유입이 0이 됨. 웹 우선이 맞는 선택 |
| 실시간 플랜 자동 재조정 | 2차 이후. 수행 데이터가 없는데 재조정할 근거가 없음 |
| 결제 / 구독 | 무명 개인 서비스의 국내 월구독 전환율은 사실상 0에 수렴 |
| 이메일 수집 및 알림 메일 | 카카오 동의항목 심사 부담 + 유출 리스크. 회원번호만으로 계정 성립 (§9.3) |
| 신체·건강 데이터 (체중·심박·부상 이력) | 민감정보 처리 책임 발생. **영구 제외** (§9.4) |

> **v1.1 변경:** "회원가입 / 로그인"은 Non-goal에서 제거되었다. 개별 훈련 정보 저장을 위해 P0로 승격하되, **플랜 생성이 아니라 저장 시점에만 요구**한다 — 상세는 §9.

---

## 4. 타겟 사용자

### P1. 첫 대회를 앞둔 입문자 (최우선)
- 대회는 신청했는데 준비 방법을 모름. "완주 가능할까?"가 최대 관심사.
- 검색어: `춘천마라톤 초보`, `하프 처음 준비`, `10km 1시간 목표`
- 필요한 것: **완주 가능성 판정**과 주차별 할 일. 페이스 존 5개는 오히려 부담.

### P2. 기록 단축을 노리는 중급자 (수익성 최고)
- 이미 완주 경험이 있고 서브4 / 서브2 같은 명확한 목표가 있음.
- 검색어: `서브4 훈련`, `하프 1시간 50분`, `VDOT`
- 필요한 것: **목표 타당성 판정**, 페이스 존, 인터벌 세션 상세. 러닝화·기어 구매력이 가장 높음 → 제휴 수익 핵심층.

### P3. 대회 탐색 중인 러너 (유입 경로)
- 아직 대회를 안 정함. "이 대회 나가면 얼마나 준비해야 하지?"
- 대회 페이지 → 플랜 생성으로 유도하는 SEO 유입층.

**MVP 우선순위: P2 > P1 > P3.** P2가 검색 의도가 뾰족하고 제휴 전환이 붙는다.

---

## 5. 핵심 사용자 흐름

```
[진입]
 ├─ A. 검색 → 대회 페이지 (/race/{slug})        ← SEO 주 유입
 ├─ B. 검색 → 목표 페이지 (/goal/{slug})        ← "서브4 훈련법" 류
 └─ C. 직접 진입 → 홈
                    ↓
        ① 대회 선택 (또는 날짜 직접 입력)
                    ↓
        ② 현재 실력 입력  ← 최근 기록 / 편한 페이스 / 모름 3분기
                    ↓
        ③ 목표 설정 + 주간 훈련 가능 일수
                    ↓
        ④ 【타당성 판정】 안정권 / 도전적 / 비현실적
              └─ 비현실적 → 달성 가능한 대안 목표 제시 → ③으로
                    ↓
        ⑤ 플랜 결과 (주차별 + 페이스표 + 코멘트)
                    ↓
        ⑥ 저장 / 수행 체크 / 캘린더 내보내기
              └─ ★ 여기서 처음 로그인 요구 (§9.2)
                 └─ 거부해도 URL 링크 복사는 가능 → 유실 없음
```

**설계 원칙: ①~⑤까지 로그인 없이 도달한다. 3화면, 60초 이내.** 이 흐름이 길어지는 순간 §3.2의 생성 완료율 60%는 불가능하다.

> **로그인은 ⑤ 이후에만 등장한다.** ①~⑤ 어디에도 인증 벽을 세우지 않는다 — 사용자 이탈뿐 아니라 SEO 크롤링도 함께 막히기 때문이다 (§9.2).

---

## 6. 기능 요구사항

### P0 — 없으면 릴리즈 불가

| ID | 기능 | 요구사항 |
|---|---|---|
| F-01 | 대회 선택 | 큐레이션된 국내 대회 검색·선택. 대회 미등록 시 **날짜 직접 입력** 폴백 필수 |
| F-02 | 실력 입력 | ① 최근 대회 기록(거리+시간) ② 편한 페이스 + 주간 거리 ③ "잘 모름"(입문자 프리셋) — 3가지 경로 |
| F-03 | 목표 설정 | 목표 완주 시간 또는 "완주만" 선택. 주간 훈련 가능 일수(3~6일) 입력 |
| F-04 | 타당성 판정 | 남은 주차 × 현재 실력 × 목표로 **안정권 / 도전적 / 비현실적** 3단계 판정 + 근거 문장 |
| F-05 | 플랜 생성 | §7 엔진 명세대로 주차별·일별 세션 생성 |
| F-06 | 플랜 뷰 | 주차 리스트(접기/펼치기) + 일별 세션 카드. 모바일 세로 기준 |
| F-07 | 페이스표 | E/M/T/I/R 5개 존의 목표 페이스를 km당 분초로 제시 |
| F-08 | 상태 저장 | 입력값을 URL에 인코딩 → 새로고침·공유·북마크로 동일 플랜 복원 (로그인 없음) |
| F-09 | SEO 페이지 | 대회별 / 목표별 정적 페이지 사전 생성 (§13) |
| F-10 | 안전 고지 | 의료 조언 아님 명시 + 통증 시 중단 안내 (§7.10) |
| **F-16** | **소셜 로그인** | **카카오 단독** (v1.3). 애플은 유료 개발자 계정이 필요해 1차에서 제외. 이메일/비밀번호 방식 없음 (§9.3) |
| **F-17** | **내 보관함 `/my`** | 저장한 플랜 목록. 기기 간 동기화의 실체 |
| **F-18** | **회원 탈퇴 + 데이터 완전 삭제** | **법적 의무. 미구현 시 릴리즈 불가** (§9.6) |
| **F-19** | **내 데이터 열람·내보내기 (JSON)** | 정보주체 권리행사 |
| **F-20** | **개인정보처리방침 / 이용약관** | 카카오 비즈앱 심사 제출물이기도 함 (§9.3) |
| **F-21** | **개인정보 보호책임자 연락처 표기** | |
| **F-12** | **주차 완료 체크** | P1 → **P0 승격.** 저장 기능의 실사용 형태이자 재방문 동기 |

### P1 — MVP에 넣으면 좋지만 미루기 가능

| ID | 기능 | 비고 |
|---|---|---|
| F-11 | 캘린더 내보내기 (.ics) | 재방문 유도 효과가 클 것으로 예상 — H1 검증에 직접 기여하므로 우선 검토 |
| F-13 | LLM 주차 코멘트 | §7.9. 없어도 플랜은 완결됨 |
| F-14 | PDF / 이미지 내보내기 | 크루 공유용 |
| F-15 | 제휴 상품 슬롯 | §14. 수익화는 트래픽 확인 후 |

### P2 — 2차 이후
장비 추천 개인화, 수행 로그 입력, 플랜 재조정, 크루 공유, GPX 업로드.

---

## 7. 플랜 생성 엔진 명세

> 이 장이 제품의 실체다. 나머지는 이 엔진의 껍데기다.
> 전 단계 **결정론적**으로 구현하며, 같은 입력 → 항상 같은 플랜이 나와야 한다. (§13의 정적 페이지 대량 생성이 이 성질에 의존)

### 7.1 입력 정규화

```ts
type PlanInput = {
  raceDate: string          // ISO date
  raceDistance: 5 | 10 | 21.0975 | 42.195
  today: string             // ISO date, 서버 기준 KST
  fitness:
    | { kind: 'race';  distance: number; timeSec: number }
    | { kind: 'feel';  easyPaceSecPerKm: number; weeklyKm: number }
    | { kind: 'novice'; canRunMin: number }   // 쉬지 않고 뛸 수 있는 시간
  goal:
    | { kind: 'time'; targetSec: number }
    | { kind: 'finish' }
  daysPerWeek: 3 | 4 | 5 | 6
  currentWeeklyKm?: number  // 미입력 시 fitness에서 추정
}
```

`weeksAvailable = floor((raceDate - today) / 7)`

### 7.2 현재 능력 추정 → VDOT

Jack Daniels의 VDOT를 단일 능력 지표로 삼는다. 5개 훈련 존이 여기서 파생되므로 지표를 하나로 통일하는 게 구현·검증 모두 유리하다.

| 입력 종류 | 처리 |
|---|---|
| `race` | 기록 → VDOT 역산 (Daniels VDOT 테이블 보간) |
| `feel` | Easy 페이스가 VDOT의 E존 범위 중앙에 오도록 역산. 주간 거리로 신뢰도 가중 |
| `novice` | 프리셋 VDOT 30~35 구간에 매핑. **타당성 판정을 항상 보수적으로** |

**거리 환산이 필요할 때 — Riegel 공식**

```
T2 = T1 × (D2 / D1)^1.06
```

**⚠️ 반드시 반영할 보정 규칙**

- Riegel은 **하프까지는 잘 맞지만**, 하프 → 풀 변환에서 **러너 절반 이상에게 10분 이상 빠르게** 나온다. 지수를 1.08로 올려도 레크리에이션 러너의 약 3/4에게 여전히 낙관적이다.
- 따라서 **목표 거리가 풀코스일 때는 Riegel 결과를 그대로 쓰지 않는다.**

```ts
// 거리 환산 시 적용 지수
const exponent = (targetDistanceKm >= 30) ? 1.10 : 1.06

// 풀코스 추정에는 추가 페널티 (주간 거리 기반)
// 주간 거리가 부족하면 후반 붕괴 리스크가 크므로 보수적으로 민다
if (targetDistanceKm >= 42 && currentWeeklyKm < 50) {
  predictedSec *= 1.03  // 초기값(가설) — 실데이터로 보정 필요
}
```

> **정직성 원칙:** 예측 결과는 항상 **범위**로 보여준다. 단일 숫자로 "당신은 3:52:10을 뜁니다"라고 말하지 않는다. 근거 없는 정밀도는 신뢰를 깎는다.

### 7.3 목표 타당성 판정 ★ 핵심 차별 기능

경쟁 생성기 대부분이 하지 않는 일이다. **불가능한 목표를 그냥 플랜으로 만들어 주는 게 가장 무책임하고, 여기서 신뢰가 갈린다.**

```
currentVDOT   = §7.2 결과
requiredVDOT  = 목표 기록을 위해 필요한 VDOT
gap           = requiredVDOT - currentVDOT
capacity      = weeksAvailable × weeklyGainRate(현재 수준, 주당 훈련 일수)
```

**weeklyGainRate 초기값 (가설 — 실데이터 확보 후 반드시 보정)**

| 현재 VDOT | 주당 VDOT 향상 (4일 훈련 기준) | 비고 |
|---|---|---|
| < 35 (입문) | 0.20 | 초기 향상폭이 가장 큼 |
| 35~45 | 0.13 | |
| 45~55 | 0.08 | |
| > 55 | 0.04 | 정체 구간 |

주당 훈련 일수 보정: `3일 ×0.75 / 4일 ×1.0 / 5일 ×1.15 / 6일 ×1.25`

**판정 로직**

| 조건 | 판정 | UI 처리 |
|---|---|---|
| `gap ≤ capacity × 0.6` | 🟢 **안정권** | 그대로 생성. "여유가 있으니 목표를 높여도 됩니다" 제안 |
| `gap ≤ capacity × 1.0` | 🟡 **도전적** | 생성하되 "빠듯합니다. 주 N회를 지키는 게 관건" 경고 |
| `gap > capacity` | 🔴 **비현실적** | **생성 차단.** 달성 가능한 대안 기록 제시 + "완주 목표로 전환" 버튼 |

**기간 자체가 부족한 경우 (별도 게이트)**

| 거리 | 최소 권장 주차 | 미달 시 |
|---|---|---|
| 5K | 6주 | 완주/컨디셔닝 플랜으로 전환 |
| 10K | 8주 | 〃 |
| Half | 10주 | 〃 |
| Full | 12주 | **완주 목표 강제.** 기록 목표 입력 차단 |

> P1(입문자) 시나리오에서 가장 자주 걸리는 분기다. 여기 문구가 제품 톤을 결정한다. 겁주지 말고, 대안을 즉시 제시할 것.

### 7.4 기간 역산 및 페이즈 배분

```
taperWeeks = { 5K: 1, 10K: 2, Half: 2, Full: 3 }
prepWeeks  = weeksAvailable - taperWeeks

Base  = round(prepWeeks × 0.45)   // 유산소 기반, E 위주
Build = round(prepWeeks × 0.35)   // T 도입, M 페이스 시작
Peak  = prepWeeks - Base - Build  // I/R, 레이스 시뮬레이션
```

**테이퍼 근거:** 마라톤에서 **3주 테이퍼가 최적**이며 **1주 테이퍼는 연구된 모든 형태 중 가장 성적이 나빴다.** 3주 엄격 테이퍼 시 기록 개선폭은 남성 1.76~2.39%, 여성 2.73~3.19%.

**테이퍼 볼륨 감량 (풀코스 3주 기준)**

| 시점 | 주간 거리 감량 | 강도 |
|---|---|---|
| D-3주 | 10~15% ↓ | **유지** |
| D-2주 | 35~40% ↓ | **유지** |
| 레이스 주 | 40~50% ↓ | 유지, 양만 축소 |

> **강도는 절대 낮추지 않는다. 양만 줄인다.** 테이퍼 설계에서 가장 많이 틀리는 지점이므로 엔진과 UI 문구 양쪽에 명시.

하프는 2주(D-2주 30%↓, 레이스 주 45%↓), 5K/10K는 1~2주로 축약 적용.

### 7.5 주간 볼륨 곡선 + ACWR 안전 가드레일 ★ 차별 포인트

경쟁 생성기 대부분은 **"주당 10% 룰"**을 쓴다. 그런데 마라톤 훈련은 장거리/단거리가 교대하는 구조여서 10% 룰이 잘 맞지 않는다는 것이 연구자들의 지적이다.

**대신 ACWR(급성:만성 부하 비율)을 가드레일로 쓴다.**

2019 뉴욕 마라톤 준비 러너 732명을 16주간 Strava로 추적한 연구:

| 조건 | 결과 |
|---|---|
| ACWR > **1.5** (주간 거리가 28일 평균의 150%) | 해당 상태 하루당 부상 위험 **+1.8%p** |
| ACWR > **1.3** | 하루당 **+0.9%p** |
| 러닝 경력·나이·성별·BMI | **부상과 무관** — ACWR만 유의했다 |

**엔진 규칙**

```ts
// acute  = 해당 주 거리
// chronic = 직전 28일 평균 주간 거리
// 생성된 플랜의 모든 주에 대해 검증
const acwr = weekKm / chronic28dAvg

if (acwr > 1.30) {
  weekKm = chronic28dAvg * 1.30   // 하드 클램프
}
```

**볼륨 곡선 규칙**

1. 시작 주간 거리 = `currentWeeklyKm` (미입력 시 fitness 기반 추정, 보수적으로)
2. 3주 증가 → 1주 감량(직전 주의 **70~80%**)의 **3:1 사이클**
3. 모든 주에 ACWR ≤ 1.30 클램프 적용
4. 피크 주간 거리 상한:

| 거리 | 피크 주간 거리 (일수별) |
|---|---|
| 5K | 25~45 km |
| 10K | 35~60 km |
| Half | 45~75 km |
| Full | 60~100 km |

5. **계산된 피크가 목표 달성에 필요한 수준에 못 미치면** → §7.3 판정을 한 단계 하향 조정 후 재판정

### 7.6 훈련 존 정의 (Jack Daniels)

| 존 | %VO2max | %HRmax | 목적 | **주간 거리 상한** | 세션 상한 |
|---|---|---|---|---|---|
| **E** Easy | 59–74% | 65–79% | 부상 저항력, 심근 강화, 모세혈관 발달 | — (기반 대부분) | 단일 런 **2.5시간** 초과 금지 |
| **M** Marathon | 75–84% | 80–90% | 레이스 페이스 감각, 글리코겐 절약 | **20%** | 1시간 50분 |
| **T** Threshold | 83–88% | 88–92% | 젖산 제거 능력 | **10%** | 템포 약 20분 또는 크루즈 인터벌 |
| **I** Interval | 95–100% | 98–100% | VO2max 향상 | **8%** | 반복 3~5분 |
| **R** Repetition | >100% | — | 무산소 파워, 러닝 이코노미 | **5%** | 반복 2분 이하 |

> **이 상한은 엔진의 하드 제약이다.** 생성된 주차가 상한을 넘으면 해당 세션을 축소하거나 E로 대체한다. 이 검증은 단위 테스트로 강제한다 (§11.3).

### 7.7 주간 세션 배치 규칙

**훈련 일수별 기본 템플릿 (Build 페이즈 기준)**

| 일수 | 구성 |
|---|---|
| 3일 | Long + Quality 1 + Easy 1 |
| 4일 | Long + Quality 1 + Easy 2 |
| 5일 | Long + Quality 2 + Easy 2 |
| 6일 | Long + Quality 2 + Easy 3 |

**배치 제약**

- 고강도(T/I/R) 세션 사이에는 **최소 이틀** 간격
- Long run 다음 날은 반드시 Easy 또는 휴식
- 주말에 Long run 기본 배치 (평일 이동 가능하게 UI에서 조정 허용 — P1)
- Long run 거리 상한: **주간 거리의 30%** 이내, 그리고 **시간 2.5시간** 이내
- 풀코스 최장 Long run: **32km 또는 3시간 중 먼저 도달하는 쪽**

**페이즈별 Quality 세션 구성**

| 페이즈 | Quality 내용 |
|---|---|
| Base | E 중심. 주 1회 Strides(짧은 R) 정도 |
| Build | T(템포/크루즈 인터벌) 도입 → M 페이스 구간 추가 |
| Peak | I(VO2max 인터벌) + 레이스 페이스 시뮬레이션 롱런 |
| Taper | 강도 유지, 볼륨만 축소 (§7.4) |

### 7.8 페이스표 산출

VDOT에서 5개 존의 목표 페이스를 km당 초 단위로 산출해 표로 제공한다. 표시 시 **±3초/km 범위**로 보여준다 (단일 숫자 금지, §7.2 정직성 원칙).

입문자(P1)에게는 **E와 M 두 개만 노출**하고 나머지는 접어 둔다. 5개 존은 인지 부하가 크다.

### 7.9 LLM 보조 레이어 — 역할 경계

**LLM이 하는 일 (문장 생성만)**

- 주차별 코멘트 1~2문장 ("이번 주는 첫 30km 롱런입니다. 페이스보다 완주에 집중하세요.")
- 타당성 판정 결과의 자연어 설명
- 대회 특성 반영 코멘트 (코스 고저, 시기별 날씨)

**LLM이 절대 하지 않는 일**

- 주간 거리, 세션 구성, 페이스, 테이퍼 비율 등 **숫자를 만들거나 바꾸는 것**
- 안전 가드레일(§7.5, §7.6) 판단

> **이유:** 숫자를 LLM이 만들면 ① 같은 입력에 다른 플랜이 나와 §13의 정적 생성이 불가능해지고 ② 상한 위반을 검증할 수 없으며 ③ 부상 리스크를 통제할 수 없다.

**비용 통제**

- 코멘트는 `(거리, 페이즈, 주차인덱스, 대회 slug)` 키로 **캐싱**. 사용자마다 호출하지 않는다.
- 정적 SEO 페이지의 코멘트는 **빌드 타임에 1회 생성**해 저장.
- 예상: 캐시 히트율 90% 이상 → 월 API 비용 수천 원 수준.

### 7.10 안전 규칙 (필수)

- 모든 플랜 화면 하단에 고정 고지: **"이 플랜은 일반적인 훈련 정보이며 의학적 조언이 아닙니다. 통증이 있으면 훈련을 중단하고 전문가와 상담하세요."**
- ACWR 클램프(§7.5)가 발동해 목표 대비 볼륨이 부족해진 경우, 사용자에게 **명시적으로 알린다.** ("안전한 증가 폭을 넘어서 조용히 줄였습니다"는 불신을 만든다)
- 임신·부상 이력·질환 관련 입력은 받지 않는다. 받으면 건강정보 처리 책임이 생긴다.

---

## 8. 데이터 모델

```ts
// 대회 (수동 큐레이션)
type Race = {
  slug: string              // 'chuncheon-marathon-2026'
  nameKo: string            // '2026 춘천마라톤'
  date: string              // ISO
  region: string            // '강원 춘천'
  distances: number[]       // [5, 10, 21.0975, 42.195]
  registrationUrl?: string
  // SEO 고유 콘텐츠용 — 자동생성 페이지 스팸 판정을 피하는 핵심 필드
  courseProfile?: 'flat' | 'rolling' | 'hilly'
  courseNote?: string       // '후반 의암호 구간 맞바람 주의'
  weatherNote?: string      // '10월 하순, 평균 최고 17도'
  cutoffHours?: number
  updatedAt: string
  sourceUrl: string         // 출처 기록 (§12 운영 원칙)
}

// 생성된 플랜 (서버 저장 없음 — URL 인코딩)
type Plan = {
  input: PlanInput
  verdict: 'safe' | 'challenging' | 'unrealistic'
  currentVDOT: number
  requiredVDOT: number
  predictedRange: [number, number]   // 초 단위 예상 기록 범위
  paces: Record<'E'|'M'|'T'|'I'|'R', { minSec: number; maxSec: number }>
  weeks: PlanWeek[]
}

type PlanWeek = {
  index: number
  phase: 'base' | 'build' | 'peak' | 'taper'
  startDate: string
  totalKm: number
  acwr: number              // 검증용 — 개발 모드에서 노출
  clamped: boolean          // ACWR 클램프 발동 여부 (§7.10 고지 트리거)
  sessions: PlanSession[]
  comment?: string          // LLM 생성, 캐시됨
}

type PlanSession = {
  dayOfWeek: 0|1|2|3|4|5|6   // 0=일
  type: 'rest' | 'easy' | 'long' | 'tempo' | 'interval' | 'repetition' | 'marathon-pace'
  distanceKm?: number
  durationMin?: number
  structure?: string        // '1.6km × 5, 회복 400m jog'
  targetZone: 'E'|'M'|'T'|'I'|'R'
}
```

**저장 전략 (v1.1 갱신):** 게스트 상태에서는 `PlanInput`을 압축·인코딩해 URL 쿼리에 담고 접속 시 재생성한다. 엔진이 결정론적이므로 가능하다 (§7 도입부).

로그인 사용자의 플랜은 여기에 더해 계정에 저장된다 (§9.7). **다만 URL 방식을 대체하는 게 아니라 얹는 것이다** — 게스트 경로와 SEO 크롤링이 그대로 살아 있어야 하고, 인증 장애 시에도 제품이 동작해야 하기 때문이다. 계정 관련 타입과 정책은 §9를 참조.

---

## 9. 계정·인증 설계

> **v1.1 변경:** v1.0에서 회원가입/로그인은 Non-goal이었으나, 개별 훈련 정보 저장을 MVP에 포함하기로 하여 P0로 승격한다. 단 **넣는 위치를 바꾼다** (§9.2).

### 9.1 결정 변경 근거

v1.0에서 로그인을 뺀 이유는 "입력 마찰"이었다. 그런데 다시 보면 그 판단은 North Star와 충돌한다.

| v1.0의 URL 저장 방식이 못 하는 것 | 결과 |
|---|---|
| 링크를 잃으면 플랜도 사라짐 | **D+7 재방문의 물리적 조건 자체가 없음** |
| 기기 간 이동 불가 (localStorage는 기기별 격리) | 폰에서 만든 플랜을 PC에서 못 봄. 러너의 실제 사용 패턴과 불일치 |
| 수행 체크(F-12) 데이터가 기기에 갇힘 | 브라우저 정리 한 번에 12주치 기록 소멸 |
| 플랜 재조정(2차 로드맵)의 전제 조건 부재 | 서버에 수행 이력이 없으면 재조정할 근거가 없음 → **2차 로드맵 전체가 막힘** |

즉 로그인을 미루는 것은 마찰을 줄이는 게 아니라 **핵심 가설 H1의 검증 수단을 없애는 것**이었다. 넣는 게 맞다.

### 9.2 핵심 원칙 — 생성 게이트가 아니라 저장 게이트

```
[게스트 · 로그인 불필요]
 대회 선택 → 실력 입력 → 목표 → 타당성 판정 → 플랜 결과 → 페이스표
                                                          │
                                                          │ 저장 / 수행 체크 /
                                                          │ 캘린더 추가 시도
                                                          ↓
                                              ┌─────────────────────┐
                                              │  여기서 처음 로그인   │
                                              └─────────────────────┘
```

**이 배치가 중요한 이유 세 가지**

1. **생성 완료율(§3.2, 목표 60%)이 로그인의 영향을 받지 않는다.** 마찰은 원래 우려한 그 자리에 안 생긴다.
2. **전환율이 가장 높은 지점에서 요청한다.** 사용자가 이미 자기 플랜을 본 뒤다. "이걸 저장하려면 로그인"은 납득되지만, "플랜을 보려면 로그인"은 이탈이다.
3. **SEO가 유지된다.** 크롤러가 플랜 결과까지 도달할 수 있어야 §13 전략이 성립한다. **콘텐츠를 로그인 벽 뒤로 넣는 것은 타협 불가 금지사항이다.**

### 9.3 인증 방식

**소셜 로그인만. 이메일·비밀번호 방식은 만들지 않는다.** (비밀번호 저장 = 유출 책임 + 재설정 플로우 + 스팸가입 대응. 개인 프로젝트가 감당할 이유가 없다)

| 제공자 | 우선순위 | 비고 |
|---|---|---|
| 카카오 | **P0** | 국내 서비스 필수. **1차 릴리즈는 이것 하나** |
| 애플 | 보류 (v1.3) | 웹의 Sign in with Apple 은 **Apple Developer Program 유료 멤버십**이 필요하다. 웹앱이라 앱스토어 강제 조항이 적용되지 않으므로 결제 시점에 재검토한다 |
| 구글 | P1 | 안드로이드·PC 보조 |

> **v1.3 — 애플 제외의 부수 효과:** O12(이메일 가리기 사용자 식별)가 존재하지 않는 문제가 되어 종결됐다. 또한 §9.3 의 심사 비대상 논거("카카오 로그인 **단독** 사용 + 회원번호만 수집")가 문자 그대로 성립한다.

**카카오 심사 — v1.2 조사 결과: 이 서비스는 심사 대상이 아닐 가능성이 높다**

이메일·전화번호 등 추가 동의항목을 받으려면 **비즈앱 전환 후 심사**를 거쳐야 한다. 제출물에 회원가입 페이지 URL, 개인정보처리방침 URL, 회원가입 화면 증빙자료, 항목별 수집 사유, 그리고 **사업자등록증 사본**이 포함된다. 심사 기간은 영업일 3~5일이며 반려 시 재신청이다.

**그런데 이 서비스는 회원번호(고유 식별자)만 받는다 (§9.4).** 카카오 개발자 문의 답변에 따르면 **카카오 로그인만 단독으로 사용하고 추가 동의항목을 신청하지 않는 경우는 심사 대상이 아니다.** 심사가 없으면 사업자등록증도 제출할 일이 없다.

> **함의 1 — 사업자등록 없이 서비스를 열 수 있다.** 개인 개발자에게 가장 큰 제약 하나가 사라진다 (O9 해소).
>
> **함의 2 — 카카오 심사가 릴리즈의 임계 경로가 아니다.** §16 일정에서 선행 접수 강제를 해제했다.
>
> **남은 확인:** 앱 등록 후 카카오 데브톡에 "카카오 로그인 단독 사용 + 회원번호만 수집" 조건을 명시해 최종 확인할 것. 앱 등록 자체는 무료다.

**이메일은 여전히 수집하지 않는다.** 회원번호만으로 계정이 성립한다. 심사 부담이 없어졌더라도 유출 리스크를 늘릴 이유가 없다. 이메일이 필요한 기능(알림 메일)은 2차로 미룬다.

### 9.4 수집 최소화 원칙

| 항목 | 수집 | 이유 |
|---|---|---|
| 소셜 제공자 고유 식별자 | **O** | 계정 식별에 필수 (해시 저장) |
| 닉네임 | 선택 | 없어도 전 기능 동작 |
| 이메일 | **X (MVP)** | 심사 부담 + 유출 리스크. 알림 기능 도입 시 재검토 |
| 프로필 이미지 | X | 불필요 |
| 생년월일 · 성별 | X | 플랜 생성에 쓰지 않음 |
| **심박수 · 체중 · 부상 이력 · 질환** | **X (영구)** | **건강정보.** 수집하는 순간 민감정보 처리 책임이 생긴다 (§7.10과 동일 기조) |

> 러닝 서비스는 건강정보 쪽으로 미끄러지기 쉽다. **훈련 기록(거리·시간·페이스)은 저장하되, 신체·건강 데이터에는 선을 긋는다.** 이 선을 넘으면 개인 프로젝트가 감당할 수 없는 영역으로 들어간다.

### 9.5 게스트 → 회원 전환

```
1. 게스트: PlanInput을 URL + localStorage(pendingPlan)에 보관
2. 저장 시도 → 로그인 리다이렉트 (state에 pendingPlan 키 포함)
3. 콜백: pendingPlan을 계정에 저장 → localStorage 정리
4. 전환 실패 시: 플랜은 URL에 그대로 있으므로 유실 없음
```

**URL 인코딩 방식을 버리지 않는다.** 로그인 이후에도 URL 복원은 계속 동작한다. **계정은 편의 레이어이지 단일 진실 공급원이 아니다.** 이 원칙 덕분에 인증 장애가 나도 제품은 계속 동작한다.

### 9.6 계정 필수 기능 — 법적 의무

개인정보처리방침에 반드시 포함해야 하는 항목: 처리 목적 / 처리·보유 기간 / 제3자 제공 / 처리 위탁 / **정보주체 권리행사 방법(열람·정정·삭제·처리정지)** / 처리 항목 / 파기 절차 / 안전성 확보 조치 / 보호책임자 연락처 / 변경 절차 / 권익침해 구제 방법.

따라서 아래는 선택이 아니라 **구현 필수**다.

| ID | 기능 | 비고 |
|---|---|---|
| F-16 | 소셜 로그인 (카카오·애플) | |
| F-17 | 내 보관함 `/my` — 저장한 플랜 목록 | |
| F-18 | **회원 탈퇴 + 데이터 완전 삭제** | **법적 의무. 미구현 시 릴리즈 불가** |
| F-19 | 내 데이터 열람·내보내기 (JSON) | 정보주체 권리행사 |
| F-20 | 개인정보처리방침 / 이용약관 페이지 | 카카오 심사 제출물이기도 함 |
| F-21 | 개인정보 보호책임자 연락처 표기 | |

**탈퇴 처리 규칙 (v1.3 확정, O11 종결):** 요청 즉시 서비스 접근 차단(soft delete) → **15일 이내 하드 삭제**. 소셜 연동 해제 포함. 탈퇴 절차는 가입만큼 쉬워야 한다 (다크패턴 금지).

유예 기간 중 같은 소셜 계정으로 재가입하면 **복구가 아니라 새 계정**이다. 이 때문에 `providerUserIdHash` 는 전역 유니크가 아니라 **활성 계정만 묶는 부분 유니크 인덱스**(`WHERE deleted_at IS NULL`)여야 한다 — 전역으로 걸면 탈퇴자가 15일 동안 재가입하지 못한다.

> ⚠️ **하드 삭제를 실제로 실행하는 스케줄러가 있어야 한다.** 방침에 15일이라 적어 놓고 실행기가 없으면 그 자체가 위반이다.

### 9.7 데이터 모델 추가

```ts
type User = {
  id: string
  provider: 'kakao' | 'apple' | 'google'
  providerUserIdHash: string    // 원본 저장하지 않음
  nickname?: string
  createdAt: string
  lastLoginAt: string
  deletedAt?: string            // soft delete → 30일 후 하드 삭제
}

type SavedPlan = {
  id: string
  userId: string
  input: PlanInput              // ★ 플랜 본문은 저장하지 않는다
  engineVersion: string         // ★ §9.7 주석 참조
  raceSlug?: string
  title: string                 // '2026 춘천마라톤 서브4'
  createdAt: string
  updatedAt: string
}

type SessionLog = {             // F-12 수행 체크
  id: string
  userId: string
  planId: string
  date: string                  // ★ v1.3 — ISO 'YYYY-MM-DD' (KST)
  status: 'done' | 'skipped' | 'modified'
  actualDistanceKm?: number
  actualDurationSec?: number
  note?: string
  loggedAt: string
}
```

> **v1.3 — `SessionLog` 의 키를 `weekIndex + dayOfWeek` 에서 `date` 로 바꿨다.** 엔진이 실제로 내주는 것이 `session.date` 이고 화면(`plan-view.ts`)도 전부 날짜로 비교하기 때문이다. 저장 타입도 `DateTime` 이 아니라 문자열이다 — 이 리포의 날짜는 전부 KST 기준 ISO 문자열이라 `DateTime` 으로 바꾸면 UTC 변환이 끼어들어 하루가 밀린다.
>
> > **`SavedPlan`이 플랜 결과 전체가 아니라 `input`만 저장한다는 점이 핵심이다.** 엔진이 결정론적이므로(§7 도입부) 입력만으로 완전 복원된다. 저장 용량이 작고, 엔진을 개선하면 저장된 플랜이 자동으로 좋아진다.
>
> **단, 이 때문에 `engineVersion`이 반드시 필요하다.** 엔진을 고치면 이미 저장된 플랜도 바뀐다. 훈련 중인 사용자의 플랜이 조용히 달라지는 것은 심각한 신뢰 훼손이다. 규칙:
> - 저장 시점의 `engineVersion`을 기록한다
> - 엔진 버전이 올라가면 기존 플랜은 **저장된 버전으로 계속 렌더링**한다
> - "개선된 플랜으로 업데이트하시겠어요?"를 **명시적으로 묻는다.** 자동 적용하지 않는다

### 9.8 인증 스택

| 항목 | 선택 | 비고 |
|---|---|---|
| 인증 라이브러리 | **Auth.js (NextAuth)** | 카카오·애플 프로바이더 기본 지원 |
| 세션 | JWT 쿠키 | `httpOnly`, `secure`, `sameSite=lax` |
| DB | **Postgres — 로컬은 Docker, 프로덕션은 Neon** (v1.3 확정) | 같은 Postgres라 스키마·마이그레이션·쿼리가 동일하고 `DATABASE_URL` 만 다르다. 서버리스는 호출마다 커넥션을 열어 풀러가 필요하고, 개인정보를 담는 DB의 백업·TLS·패치를 1인이 떠안지 않기 위해 프로덕션은 관리형으로 간다 |
| ORM | **Prisma** (v1.3 확정) | 스키마가 3테이블·조인 거의 없음이라 ORM 간 성능차가 드러나지 않는다. 판단 기준은 "6개월 뒤에 읽을 수 있는가"이고 `schema.prisma` 단일 선언 파일이 이 리포의 인수인계 기조와 맞는다 |
| 식별자 | `providerUserId`는 해시하여 저장 | 유출 시 피해 축소 |

### 9.9 새로 생기는 측정 지표

| 지표 | 목표 | 의미 |
|---|---|---|
| 게스트 플랜 생성 → 로그인 전환율 | ≥ 25% | 저장 게이트가 제 위치에 있는지 |
| **로그인 사용자 D+7 재방문율 vs 게스트** | 비교 관측 | **H1을 훨씬 정확히 측정 가능** |
| 저장 플랜당 주차 체크 발생률 | ≥ 30% | 수행 추적이 실제로 쓰이는지 |
| 탈퇴율 | 관측 | 신뢰 이슈 조기 감지 |

> 로그인을 넣는 진짜 이득은 저장 기능 자체보다 **H1을 제대로 측정할 수 있게 된다는 것**이다. 게스트만으로는 재방문을 정확히 셀 수 없었다.

### 9.10 다른 장에 미치는 영향

| 장 | 변경 |
|---|---|
| §3.2 성공 기준 | §9.9 지표 추가 |
| §3.3 Non-goals | "회원가입/로그인" 항목 제거 |
| §6 기능 요구사항 | P0에 F-16~F-21 추가. F-12(수행 체크)를 P1 → **P0 후보**로 상향 |
| §8 데이터 모델 | §9.7 타입 추가. "DB에 저장하지 않는다" 서술 갱신 |
| §10 화면 명세 | `/my` 보관함, `/settings`(탈퇴·데이터), `/privacy`, `/terms` 추가 |
| §11 기술 아키텍처 | Postgres + Auth.js 추가 |
| §16 릴리즈 계획 | **6주 → 8주.** 인증·법적 문서·카카오 심사 반영 |
| §17 리스크 | R9(개인정보), R10(카카오 심사 지연) 추가 |

---

## 10. 화면 명세 (모바일 우선)

기준 뷰포트 **375×667**. 모든 화면은 세로 스크롤 한 방향, 가로 스크롤 금지.

### 10.1 홈 `/`
- 히어로: "대회 날짜를 넣으면, 오늘부터 뭘 해야 할지 알려드립니다"
- 대회 검색 입력 (자동완성) + "대회가 목록에 없어요" 링크
- 다가오는 주요 대회 카드 6개 (D-day 표시) — 내부 링크로 SEO 크롤 경로 확보

### 10.2 대회 페이지 `/race/{slug}` ★ SEO 주 진입점
- 대회 개요 (날짜, 장소, 종목, D-day 카운터)
- **코스·날씨 고유 콘텐츠** (`courseNote`, `weatherNote`) — 자동생성 페이지가 아니라는 신호
- 종목별 "이 대회 목표 플랜 만들기" CTA
- 목표 기록별 페이스 표 (서브4 / 서브3:30 등 3~4개 프리셋)

### 10.3 입력 스텝 `/plan/new`
3스텝 progressive disclosure. 스텝당 질문 2개 이하.

| 스텝 | 질문 |
|---|---|
| 1 | 대회/날짜 (사전 선택 시 스킵) |
| 2 | 현재 실력 — 3분기 선택 후 해당 입력만 노출 |
| 3 | 목표 기록 + 주당 훈련 일수 |

- 각 스텝 하단에 진행 표시. 뒤로 가기 시 입력 보존.
- **숫자 입력은 전부 `inputmode="numeric"`.** 페이스·시간 입력은 `mm:ss` 마스크.

### 10.4 판정 화면 `/plan/verdict`
- 판정 배지(🟢🟡🔴) + 한 문장 근거
- 🔴일 때: 달성 가능한 대안 기록 **2개** 제시 + "완주 목표로" 버튼. 되돌아가기 부담 없게.
- 🟢일 때: 상향 목표 제안 1개

### 10.5 플랜 결과 `/plan/result`
- 상단 고정: D-day, 목표, 총 주차, 예상 기록 **범위**
- 페이스표 (P1은 E/M만, 나머지 접기)
- 주차 리스트: 아코디언. 현재 주차 자동 펼침 + 스크롤 앵커
- 주차 카드: 페이즈 뱃지 / 총 거리 / 일별 세션 7칸 / LLM 코멘트
- ACWR 클램프 발동 주차에 안내 배지 (§7.10)
- 하단 고정 바: **저장(링크 복사) / 캘린더 추가 / 공유**

### 10.6 상태 저장
- `PlanInput` → base64url 인코딩 → `/plan/result?p={...}`
- 링크 복사 시 "이 링크를 저장하면 언제든 다시 볼 수 있어요" 토스트
- 브라우저 `localStorage`에 최근 플랜 3개 캐시 (편의 목적, 유실 허용)

### 10.8 계정 화면 (v1.1 추가)

| 경로 | 내용 |
|---|---|
| `/login` | 카카오·애플 버튼만. 별도 가입 폼 없음. **"플랜을 저장하려면 로그인이 필요합니다"** 맥락 문구 필수 |
| `/my` | 저장한 플랜 카드 목록 (대회명, D-day, 진행률). 진행 중 플랜 최상단 고정 |
| `/settings` | 연결된 소셜 계정 / 데이터 내보내기(F-19) / **회원 탈퇴**(F-18) |
| `/privacy` | 개인정보처리방침 (F-20). 카카오 심사 제출 URL |
| `/terms` | 이용약관 (F-20) |

**로그인 모달 문구 원칙**
- ❌ "로그인이 필요합니다" — 이유가 없다
- ✅ "이 플랜을 저장하고 어느 기기에서든 이어보려면 로그인해 주세요"
- 모달에는 항상 **"링크만 복사하기"** 대안을 함께 둔다. 로그인을 거부해도 플랜을 잃지 않는다는 걸 즉시 보여준다 (§9.5)

### 10.9 접근성·성능 기준
- LCP < 2.0s (4G 기준), CLS < 0.1
- 광고 슬롯은 **고정 높이 예약** — CLS 방어
- 폰트: 시스템 폰트 스택 우선, 웹폰트는 subset만

---

## 11. 기술 아키텍처

### 11.1 스택 제안

| 레이어 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js (App Router)** | SSG/ISR로 SEO 페이지 대량 생성 (§13). 이게 결정적 |
| 언어 | TypeScript | 엔진 로직의 타입 안정성이 곧 안전 가드레일 |
| 스타일 | Tailwind | 모바일 우선 반응형 속도 |
| 엔진 | **순수 TS 패키지로 분리** (`packages/engine`) | §11.2 참조 |
| 대회 데이터 | JSON 파일 (빌드 타임 로드) | 50개 규모라 DB에 넣지 않는다. 계정 DB와 분리 유지 |
| LLM | 서버 라우트 + 파일/KV 캐시 | §7.9 비용 통제 |
| **인증** | **Auth.js (NextAuth)** | **카카오·애플 프로바이더 (§9.8)** |
| **DB** | **Postgres — Docker(로컬) / Neon(프로덕션)** | **v1.1에서 추가, v1.3에서 확정. 계정·저장 플랜·수행 로그** |
| **ORM** | **Prisma** (`packages/db`) | **v1.3 확정 (§9.8)** |
| 배포 | Vercel | ISR 네이티브 지원 |

### 11.2 핵심 아키텍처 결정: 엔진 분리

```
packages/engine/          ← 의존성 없는 순수 함수. I/O 없음
  vdot.ts                 VDOT 테이블, 페이스 산출
  riegel.ts               거리 환산 + 보정 (§7.2)
  feasibility.ts          타당성 판정 (§7.3)
  periodization.ts        페이즈 배분, 볼륨 곡선, ACWR (§7.4~7.5)
  sessions.ts             주간 세션 배치 (§7.7)
  index.ts                generatePlan(input): Plan

apps/web/                 Next.js
```

**이 분리가 주는 것:**
1. 빌드 타임에 같은 함수를 호출해 **정적 SEO 페이지 수백 개 생성** 가능
2. 엔진만 단위 테스트 — 브라우저 없이 수천 케이스 검증
3. 나중에 네이티브 브리지 앱을 만들어도 엔진 재사용

### 11.3 필수 테스트 (엔진 불변식)

플랜 생성 결과에 대해 **모든 생성 케이스에서** 다음을 검증한다:

```ts
// property-based test — 입력 조합을 무작위 생성해 검증
✓ 모든 주차의 ACWR ≤ 1.30
✓ T존 주간 거리 ≤ 총 거리의 10%
✓ I존 ≤ 8%,  R존 ≤ 5%,  M존 ≤ 20%
✓ 고강도 세션 간 최소 2일 간격
✓ 단일 Easy/Long 런 ≤ 2.5시간
✓ Long run ≤ 주간 거리의 30%
✓ 풀코스 최장 Long run ≤ 32km
✓ 테이퍼 구간 강도 존이 직전 페이즈보다 낮아지지 않음
✓ 같은 입력 → 같은 출력 (결정론)
✓ weeksAvailable 1~52 전 구간에서 크래시 없음
```

> 이 테스트 스위트가 곧 §7.10 안전 규칙의 실행 형태다. 여기 통과하지 못하면 배포하지 않는다.

---

## 12. 대회 데이터 운영

**MVP: 수동 큐레이션 50개.**

**선정 기준**
1. 참가자 수 상위 대회 (춘천, JTBC, 서울, 동아, 공주, 경주 등)
2. 향후 12개월 내 개최
3. 5K/10K/하프/풀 중 2종목 이상 운영

**운영 원칙**
- 대회당 필수 필드: 날짜, 장소, 종목, 출처 URL, `updatedAt`
- **차별화 필드(`courseNote`, `weatherNote`)를 반드시 1개 이상 채운다.** 이게 없으면 §13의 SEO 전략이 무너진다
- 갱신: 월 1회 수동 점검. 대회 3개월 전부터는 격주
- 날짜 변경/취소 리스크 → 각 페이지에 "최종 확인: YYYY-MM-DD" + 공식 사이트 링크 명시

**크롤링을 하지 않는 이유 (MVP 한정)**
- 대상 사이트 이용약관 및 차단 대응 유지보수 비용
- 50개 규모에서 자동화 ROI가 없음
- **데이터 품질이 곧 SEO 신뢰도**인데 크롤링은 오히려 품질을 떨어뜨림

→ H2(§3.1)가 검증되고 대회 페이지 트래픽이 확인되면 그때 자동화를 재검토한다.

---

## 13. SEO 전략

**전제: 이 서비스의 유입은 90% 검색이다.** 네이티브 앱을 만들지 않는 이유이기도 하다.

### 13.1 페이지 구조

| 유형 | URL | 생성 방식 | 예상 규모 |
|---|---|---|---|
| 대회 | `/race/{slug}` | SSG | 50 |
| 대회 × 종목 | `/race/{slug}/{distance}` | SSG | ~150 |
| 목표 기록 | `/goal/{slug}` (`sub4`, `sub2-half`) | SSG | ~15 |
| 대회 × 목표 | `/race/{slug}/{goal}` | **SSG, 선별적** | ~100 |
| 도구 | `/tools/pace`, `/tools/vdot` | SSG | 5 |

### 13.2 ⚠️ 자동생성 페이지 리스크 관리

`대회 × 목표` 조합은 이론상 수천 개까지 늘릴 수 있지만 **그렇게 하면 안 된다.** 저품질 대량생성 페이지로 판정되면 사이트 전체가 죽는다.

**제한 규칙**
1. 조합 페이지는 **100개 이하**로 시작
2. 각 페이지는 §12의 대회 고유 콘텐츠(코스·날씨·전략)를 반드시 포함
3. 목표별로 **다른 문단**을 쓴다 — 템플릿에 숫자만 바꾼 페이지 금지
4. 유입이 없는 조합은 3개월 뒤 `noindex` 처리
5. `sitemap.xml`은 실제 가치 있는 페이지만 등록

### 13.3 구조화 데이터
- 대회 페이지: `SportsEvent` 스키마 (날짜·장소·URL)
- 도구 페이지: `HowTo` 또는 `FAQPage`
- AI 검색(GEO/AEO) 대비: 각 페이지 상단에 **질문형 H1 + 2~3문장 직답** 배치

### 13.4 콘텐츠 클러스터
플랜 생성기 단독으로는 권위가 안 생긴다. 대회 페이지를 허브로 삼고 주변 글을 붙인다:
`서브4 페이스 전략` / `첫 하프 완주 가이드` / `테이퍼링 하는 법` / `대회 전날 체크리스트` → 모두 대회 페이지와 플랜 생성기로 내부 링크.

---

## 14. 수익화

**구독은 하지 않는다** (§3.3). MVP 수익 구조는 두 갈래.

### 14.1 디스플레이 광고
- 애드센스. 러닝 니치 CPC는 낮은 편이므로 **주 수익원으로 기대하지 않는다**
- 배치: 플랜 결과 페이지 주차 리스트 중간(3주차마다 1개), 대회 페이지 하단
- **고정 높이 예약 필수** (§10.7 CLS)

### 14.2 제휴 — 이쪽이 본체
쿠팡 파트너스 기반. **플랜 안의 자연스러운 문맥 지점**에만 붙인다.

| 문맥 지점 | 추천 카테고리 |
|---|---|
| 주간 거리 40km 돌파 주차 | 데일리 트레이너 러닝화, 로테이션 개념 |
| 첫 30km 롱런 주차 (풀코스) | 에너지 젤, 보급 전략 |
| 테이퍼 진입 주차 | 카본화, 레이스 데이 장비 |
| 판정 결과가 🔴인 사용자 | 초보용 쿠셔닝화, 기초 가이드 |
| 겨울 대회 플랜 | 방한 장비 |

**원칙: 플랜의 흐름을 끊지 않는 위치에만. "추천"이라고 명시하고 제휴 고지를 넣는다.** 신뢰가 이 제품의 유일한 자산이다.

### 14.3 수익 기대치 (보수적)
1차 목표는 수익이 아니라 §3.2의 재방문율 검증이다. 광고·제휴는 트래픽이 월 1만 세션을 넘긴 뒤 최적화한다.

---

## 15. 측정 계획

| 이벤트 | 속성 |
|---|---|
| `plan_start` | 진입 경로(race/goal/direct), 대회 slug |
| `fitness_input` | 입력 방식(race/feel/novice) |
| `verdict_shown` | 판정(safe/challenging/unrealistic), gap, weeksAvailable |
| `goal_adjusted` | 이전 목표 → 새 목표 (🔴 이후 재조정률 = H3 검증) |
| `plan_generated` | 거리, 주차 수, 판정, 주당 일수 |
| `plan_saved` | 방식(link/calendar/pdf) |
| `plan_revisit` | **최초 생성으로부터 경과일 → H1 검증 핵심** |
| `week_checked` | 주차 인덱스, 상태(done/skipped/modified) |
| `login_prompted` | 트리거(save/check/calendar) — **저장 게이트가 어디서 걸리는지** |
| `login_completed` | 제공자(kakao/apple), 게스트→회원 전환 소요 시간 |
| `login_dismissed` | 트리거. 링크 복사로 대체했는지 여부 |
| `plan_migrated` | 게스트 플랜 → 계정 이전 성공/실패 (§9.5) |
| `account_deleted` | 가입 후 경과일 — 탈퇴율 조기 감지 |
| `affiliate_click` | 문맥 지점, 카테고리 |

**대시보드 1순위: 생성일 기준 코호트별 D+1/D+3/D+7/D+14 재방문 곡선.** 다른 건 다 부차적이다.

**대시보드 2순위 (v1.1 추가): 게스트 vs 로그인 사용자의 재방문 곡선을 나란히.** 두 곡선의 격차가 로그인을 넣은 판단이 옳았는지에 대한 답이다. 격차가 없다면 계정은 비용만 늘린 셈이므로, 2차에서 인증 투자를 늘리지 않는다.

---

## 16. 릴리즈 계획

### 1차 릴리즈 — 8주 (v1.1: 인증 도입으로 6주 → 8주)

| 주차 | 작업 | 완료 기준 |
|---|---|---|
| W1 | 엔진 코어: VDOT, Riegel 보정, 페이스 산출 | §11.3 테스트 중 페이스/환산 항목 통과 |
| W2 | 엔진: 타당성 판정, 페이즈 배분, ACWR, 세션 배치 | **§11.3 전 항목 통과** |
| W3 | 대회 데이터 50개 + 입력 스텝 UI (F-01~03) | 실제 대회로 플랜 생성 성공 |
| W4 | 판정 화면 + 플랜 결과 뷰 + 페이스표 (F-04~08) | **게스트 전 흐름 완주** (모바일 375px) |
| **W5** | SEO SSG + 구조화 데이터 + **개인정보처리방침·이용약관 작성 (F-20, F-21)** | Lighthouse SEO 100 / **방침 페이지 공개 가능 상태** |
| **W6** | Auth.js 인증 + 보관함 + 탈퇴·삭제 (F-16~F-19) | **카카오** 로그인 동작, **탈퇴 시 데이터 완전 삭제 검증**(하드 삭제 Cron 포함) |
| **W7** | 주차 체크(F-12) + 게스트→회원 이전(§9.5) + LLM 코멘트 + 계측 | 이전 실패 시 URL 폴백 동작 확인 |
| W8 | 도그푸딩 반영 + 안전 고지 + 베타 배포 | 본인 플랜으로 2주 실사용 완료 |

> **v1.2 변경 — 카카오 심사가 임계 경로에서 빠졌다.** 회원번호만 수집하면 심사 대상이 아님을 확인했다 (§9.3). 사업자등록 없이도 진행 가능하다.
>
> **다만 개인정보처리방침(F-20)은 여전히 W5에 완성한다.** 심사 제출물이라서가 아니라 **법적 의무**이고, 인증을 켜는 순간 개인정보 처리가 시작되기 때문이다. 방침 없이 로그인을 여는 것이 실제 위반이다.

### 도그푸딩 — 이 제품의 가장 큰 자산

작성자 본인이 실제 러너이므로 **첫 사용자로 직접 검증한다.**

| 대회 | 날짜 | 오늘(2026-09-07) 기준 | 검증 대상 |
|---|---|---|---|
| MBN 서울 마라톤 (하프) | 2026-11-15 | **D-10주** | 하프 경로 전체. W3 완료 시점부터 실제 사용 시작 |
| 도쿄 마라톤 (풀) | 2027-03-07 | **D-26주** | 풀코스 경로 + 장기 플랜. 릴리즈 후 실사용 |

> 하프까지 10주는 §7.3 최소 권장 주차(10주)에 정확히 걸리는 케이스다. **경계값을 본인이 직접 밟는다.** 판정 문구와 볼륨 곡선을 실감으로 검증할 수 있는 기회.

### 2차 이후 (H1 통과 시에만)
수행 로그 상세 입력 → **플랜 자동 재조정**(계정 도입으로 이제 가능해짐) → GPX 업로드 → 이메일 알림(동의항목 심사 필요) → 크루 공유 → (검토) 네이티브 브리지 앱으로 HealthKit 연동.

---

## 17. 리스크 및 대응

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | **재방문율 20% 미달** | 제품화 실패 | 사전에 판단 기준 확정(§3.2). 실패 시 도구+광고로 착지 — 손실 한정 |
| R2 | 웹이라 기록 자동 연동 불가 | 이탈 요인 | MVP에서 **아예 시도하지 않음**. 수동 입력 최소화 설계로 회피 (§3.3) |
| R3 | 잘못된 플랜으로 인한 부상 | 신뢰·법적 | §7.5 ACWR 클램프 + §7.6 존 상한 + §11.3 테스트 강제 + §7.10 고지 |
| R4 | 자동생성 페이지 SEO 페널티 | 유입 0 | §13.2 제한 규칙. 조합 100개 상한, 고유 콘텐츠 필수 |
| R5 | 대회 정보 오류(날짜 변경·취소) | 신뢰 | 출처 링크 + 최종 확인일 명시 + 월 1회 점검 (§12) |
| R6 | Runna 등이 한국 대회 지원 | 차별점 소멸 | 한국어 콘텐츠 + 국내 대회 커버리지 깊이로 방어. 다만 **방어 가능성은 근본적으로 낮음** — 그래서 저비용·단기 검증 구조를 택함 |
| R7 | LLM 비용 폭증 | 운영 | §7.9 캐싱. 최악의 경우 코멘트 기능만 끄면 제품은 동작 |
| **R9** | **개인정보 유출 / 처리 위반** | **법적·신뢰 치명** | 수집 최소화(§9.4) — 이메일·건강정보 미수집이 최선의 방어. 식별자 해시 저장. 탈퇴·삭제(F-18) 실동작 검증을 릴리즈 조건으로 |
| R10 | 카카오 비즈앱 심사 지연·반려 | 일정 | **v1.2에서 위험도 하향.** 회원번호만 수집하면 심사 대상이 아님을 확인 (§9.3). 만약 심사가 필요해지면 **애플 로그인만으로 먼저 오픈**하고 카카오를 후속 추가. 게스트 경로가 살아 있으므로 서비스 자체는 막히지 않음 |
| **R11** | **엔진 개선 시 저장된 플랜이 바뀜** | 신뢰 | `engineVersion` 고정 렌더링 + 업데이트 명시적 동의 (§9.7) |
| **R12** | **로그인 도입으로 생성 완료율 하락** | 핵심 지표 | 저장 게이트 배치(§9.2)로 구조적 회피. `login_prompted`/`login_dismissed` 계측으로 조기 감지 |
| R8 | 개인 프로젝트 지속성 | 방치 | 엔진이 순수 함수 + 정적 페이지 → 운영 개입 없이도 동작. 대회 데이터만 분기별 갱신하면 유지 가능. **단 v1.1부터는 계정 데이터가 생겨 "그냥 방치"가 불가능하다** — 서비스 종료 시 데이터 파기·고지 의무 발생 |

---

## 18. 오픈 이슈

### 해결된 이슈 (v1.2 기준)

| # | 이슈 | 결론 |
|---|---|---|
| O1 | 제품명 및 도메인 | **제품명 RUNBACK 확정.** 도메인은 미정 — 개인정보처리방침의 연락처 주소가 여기 달려 있어 다음 병목이다 |
| O2 | `weeklyGainRate` 근거 | **해결.** 문헌 3편으로 앵커를 잡고 안전계수 0.5를 적용한 연속 감쇠 모델로 대체. PRD 의 4구간 계단 함수는 폐기 → `docs/adr/0002-vdot-gain-rate.md` |
| O3 | VDOT 테이블 저작권 | **해결.** 원 표를 복제하지 않고 Daniels-Gilbert 공개 회귀식 2개로 자체 구현. 리포지토리에 표에서 옮긴 상수가 없다 → `docs/adr/0001-vdot-model.md` |
| O4 | 입문자 페이스 존 노출 개수 | **E·M 2개로 확정.** 엔진 `visibleZones('novice')` 로 구현 |
| O7 | 대회 선정 최종 리스트 | **82개 큐레이션 완료** → `docs/race-data-operations.md`. 다만 국내 일정 공개 지평선이 약 7개월이라 §12 의 "향후 12개월" 기준은 충족 불가. 롤링 윈도우로 운영한다 |
| **O8** | 개인정보 보호책임자 표기 | **해결.** 개인정보보호법 제30조 제1항이 "성명 **또는** 부서의 명칭과 연락처"를 요구하므로 실명 대신 **닉네임 `sseong`** 으로 표기한다. 상시 근로자 5명 미만이라 별도 지정 의무도 없고 운영자 본인이 보호책임자가 된다. **연락처는 생략 불가** — 서비스 전용 이메일로 대응 (도메인 확정 후) |
| **O9** | 사업자 등록 필요 여부 | **해결 — 지금은 불필요.** 카카오 심사가 대상이 아니므로 사업자등록증 제출 상황이 없다 (§9.3). 광고·제휴 수익이 계속 발생하면 사업소득 규모에 따라 등록 의무가 생길 수 있으나 별개 사안이다 |
| **O15** | 대회 탭 정보구조 | **해결.** 대회 탭 = **내 대회(등록한 목표)**, 전체 대회 브라우징은 탭바 없는 공개 라우트 `/race` 로 분리했다. 검색 유입자에게 하단 탭은 소음이고, 로그인 사용자가 그 탭에 오는 이유는 '무슨 대회가 있나'가 아니라 '내가 준비 중인 대회가 어떻게 되고 있나'이기 때문이다. 훈련 일정 상세는 `/races/{slug}` 중첩 라우트로 두어 상세에서도 대회 탭이 활성 상태로 남는다 |

### 남은 이슈

| # | 이슈 | 결정 필요 시점 |
|---|---|---|
| ~~O1'~~ | ~~도메인 확정~~ — **종결 (2026-09-10).** `runback.kr` 확정, `privacy@runback.kr` 을 보호책임자 연락처로 기재. 남은 것은 방침 **시행일**뿐이고 이는 인증을 켜는 날에 정해진다 | 종결 |
| O5 | F-11 캘린더 내보내기를 MVP에 포함할지 — H1 검증 기여도가 높아 보임 | W7 |
| O6 | 러닝화 제휴에서 카본화 추천 기준 (실력 수준별 컷오프) | 2차 |
| **O10** | 게스트 플랜의 URL 유효기간 — 무기한 유지할지, 엔진 버전 변경 시 어떻게 처리할지 (§9.7 `engineVersion`과 연결) | W7 |
| ~~O11~~ | ~~탈퇴 유예 기간~~ — **종결 (2026-09-10).** **15일** soft delete 후 하드 삭제 (§9.6) | 종결 |
| ~~O12~~ | ~~애플 이메일 가리기 사용자 식별~~ — **종결 (2026-09-10).** 애플 로그인을 1차에서 제외해 문제 자체가 사라졌다 (§9.3) | 종결 |
| **O13** | **성별 수집 여부** — UI 목업에 항목이 있으나 §9.4 는 미수집으로 정해 두었고 엔진도 쓰지 않는다. 받으려면 §7.4 의 성별별 테이퍼 차이를 엔진에 반영해 "왜 받는가"에 답이 있어야 한다. 받아만 두고 안 쓰면 수집 최소화 위반 | W6 |
| **O14** | **디바이스 연동** — UI 목업에 메뉴가 있으나 §3.3 Non-goal. 애플·삼성 헬스는 웹에서 물리적으로 불가(네이티브 브리지 필수), Strava 는 본인 데이터 읽기만이라면 가능성 있음. 누르면 아무것도 안 되는 메뉴는 신뢰를 깎으므로 넣지 않거나 제대로 하거나 둘 중 하나 | 2차 |
| **O16** | **여러 목표 동시 관리** — 화면(대회 탭 목록 + 추가 슬롯)은 배열 기준으로 열어 뒀으나 **엔진·URL 구조는 아직 플랜 1개 기준**이다 | W7 |
| **O17** | **진행률 기준** — 표시 기준은 세션 완료 수(`12/15`)로 교체 완료. 다만 "완료" 판정을 아직 날짜 경과로 대신하고 있어, F-12 수행 기록이 붙어야 진짜가 된다 (`sessionProgress()` 안쪽만 바꾸면 됨) | W7 |

> **v1.3 — 릴리즈 블로커였던 도메인(O1')이 닫혔다**
>
> v1.1 이 지목한 블로커 세 개(O3·O8·O9)와 v1.2 의 남은 블로커 O1' 이 모두 해소됐다. 도메인은 `runback.kr` 로 확정했고 보호책임자 연락처는 `privacy@runback.kr` 이다.
>
> **다만 방침이 아직 공개 가능 상태는 아니다.** `LEGAL.effectiveDate` 가 비어 있고, 시행일은 실제로 개인정보 처리를 시작하는 날(= 카카오 로그인을 켜는 날)에 정해진다. `hasUnresolvedLegalInfo()` 가 그때까지 `true` 로 남아 신호 역할을 한다.
>
> **새로 생긴 필수 항목 둘**
> - **국외 이전 고지 (법 제28조의8)** — 호스팅(Vercel)·DB(Neon)가 국외 사업자라 방침에 이전받는 자·국가·항목·목적·보유기간을 적어야 한다. `legal-content.ts` 제7항에 반영했다.
> - **하드 삭제 스케줄러** — 방침이 "15일 이내 삭제"를 약속하므로 이를 실제로 실행하는 Cron 이 F-18 의 완료 조건이다.

---

## 부록 A. 참고 자료

**시장**
- [마라톤 대회 참가자 100만 명 돌파 (우먼타임스)](https://www.womentimes.co.kr/news/articleView.html?idxno=103164)
- [러닝 인구 1천만 시대 (데일리환경)](https://www.dailyt.co.kr/newsView/dlt202603300002)

**경쟁**
- [Strava의 Runna 인수 발표](https://press.strava.com/articles/strava-to-acquire-runna-a-leading-running-training-app)
- [Runna 인수 이후 변화 분석](https://therunninggenie.com/blog/is-runna-still-worth-it-after-strava-acquisition)
- [나이키 코리아 마라톤 트레이닝 플랜](https://www.nike.com/kr/running/marathon-training-plan)
- [러너블](https://runable.me/)

**기술 제약**
- [Strava API 약관 변경 — 제3자 앱 데이터 표시 제한](https://support.strava.com/en-us/articles/15401608-api-agreement-update-how-data-appears-on-3rd-party-apps)
- [웹앱의 Apple Health 접근 제약](https://www.themomentum.ai/blog/do-you-need-a-mobile-app-to-access-apple-health-data)

**계정·인증 (v1.1)**
- [카카오 로그인 사전 설정 및 동의항목 심사 (카카오 디벨로퍼스)](https://developers.kakao.com/docs/ko/kakaologin/prerequisite)
- [개인정보처리방침 필수 기재사항 (개인정보포털)](https://www.privacy.go.kr/front/contents/cntntsView.do?contsNo=121)
- [개인정보보호법 (국가법령정보센터)](https://www.law.go.kr/%EB%B2%95%EB%A0%B9/%EA%B0%9C%EC%9D%B8%EC%A0%95%EB%B3%B4%EB%B3%B4%ED%98%B8%EB%B2%95)

**훈련 방법론**
- [Jack Daniels 훈련 강도 존 정의 (Coach Ray)](https://www.coachray.nz/2023/05/03/jack-daniels-running-intensity/)
- [Riegel 공식과 정확도 한계 (FitMetricLab)](https://fitmetriclab.com/blog/riegel-formula-race-prediction/)
- [마라톤 테이퍼 길이 연구 정리 (Marathon Handbook)](https://marathonhandbook.com/optimal-marathon-taper-length/)
- [훈련량 증가와 부상 위험 — ACWR 연구 (HSS, NYC 마라톤 러너 732명)](https://news.hss.edu/hss-study-shows-greater-increases-in-training-volume-associated-with-higher-risk-of-injuries-among-marathon-runners/)
- [10% 룰의 한계 (Marathon Handbook)](https://marathonhandbook.com/the-10-rule-new-study-suggests-weve-been-doing-it-wrong-this-whole-time/)
