# CLAUDE.md — 인수인계 문서

이 파일 하나만 읽고 바로 이어서 작업할 수 있게 쓴 문서다.
새 세션은 **여기 → `prd-raceback-mvp_1.md`(필요한 절만) → 해당 소스** 순서로 읽는다.

마지막 갱신: 2026-09-11 (입문자 세션 수행 가이드 §7.8-1 + 대회 달력 + E2E 141개)

---

## 0. 절대 규칙

1. **커밋하지 않는다.** 작업이 끝나면 커밋 메시지만 사용자에게 전달하고, 커밋은 사용자가 직접 한다.
   `git add` / `git commit` / `git push` 를 실행하지 말 것.
2. **PRD 가 상위 문서다.** 코드가 PRD 와 어긋나면 둘 중 하나를 고치되, 말없이 어긋난 채로 두지 않는다.
   PRD 를 바꿨으면 §18 오픈 이슈 표도 같이 갱신한다.
3. **숫자를 지어내지 않는다.** 수행 기록(실제로 뛴 거리·페이스·기분)은 아직 존재하지 않는다.
   화면을 채우려고 가짜 러닝 로그를 만들지 말 것. 빈 상태로 두는 편이 정확하다.
4. **엔진에 Node 의존성을 넣지 않는다.** `packages/engine` 은 런타임 의존성 0이 계약이고,
   `tsconfig.purity.json` 이 이걸 강제한다.
5. **명도 대비는 눈으로 고르지 않는다.** 색을 건드렸으면 `npm run contrast` 를 통과해야 한다.

---

## 1. 제품 한 줄

**RUNBACK** — 국내 대회 날짜에서 **역산해서** 주차별 러닝 훈련 플랜을 만들어 주는 모바일 우선 웹서비스.

> "11월 15일 하프까지 남은 10주, 목표 1:55 기준 플랜"

차별점은 플랜 생성 자체가 아니라 **① 국내 대회 날짜 역산**과 **② 목표 타당성 판정**(안정권/도전적/비현실적)이다.
"기록 입력 → 플랜 생성" 일반형은 이미 포화 시장이라는 판단에서 나온 축이다.

---

## 2. 문서 지도

| 파일 | 내용 | 언제 읽나 |
|---|---|---|
| `CLAUDE.md` (이 파일) | 인수인계·현재 상태·규칙 | 항상 먼저 |
| `prd-raceback-mvp_1.md` | 제품 정의 **v1.8**. 1200줄+ | 해당 절만 발췌해서 |
| `README.md` | 사람이 읽는 소개 + 개발 명령 | 명령어 확인 |
| `docs/adr/0001-vdot-model.md` | VDOT 저작권 회피 결정 (O3) | 엔진 페이스 손댈 때 |
| `docs/adr/0002-vdot-gain-rate.md` | 향상률 모델 근거 (O2) | 엔진 판정 손댈 때 |
| `docs/race-data-operations.md` | 대회 데이터 수집·검증 운영 | 대회 추가·갱신할 때 |
| `docs/qa-sheet.md` | **수동 QA 체크리스트** (PRD v1.6 기준) + 알려진 미해결 목록 | 배포 전 · 회귀를 의심할 때 |
| `docs/mockups/` | 작성자가 그린 **화면 목업** (+ 파일 이름 규칙·용량 주의) | 화면을 새로 짜거나 고칠 때 |

PRD 에서 자주 쓰는 절: §7(엔진 명세) · §9(계정·개인정보) · §10(화면) · §11.3(불변식) · §13(SEO) · §18(오픈 이슈)

---

## 3. 저장소 구조

```
packages/engine/     플랜 생성 엔진. 순수 함수, 런타임 의존성 0, ENGINE_VERSION '0.3.0'
packages/races/      국내 대회 큐레이션 82개 + 검증
packages/db/         Prisma + Postgres. 계정·저장 플랜·수행 기록 (2026-09-10 신설)
apps/web/            Next.js 16.3.4 / React 19.2 / Tailwind 4.3.3
apps/web/public/illustrations/  세션 씬 아트워크 남/여 각 10장 (배포 자산)
test/                패키지를 가로지르는 통합 테스트
scripts/             check-contrast.ts (명도 대비), check-races.ts (대회 데이터 점검)
docs/adr/            설계 결정 기록
```

pnpm 모노레포. Node **22.18 이상** 필요 (엔진이 Node 내장 타입 스트리핑으로 `.ts` 를 그대로 실행한다).

### 엔진 (`packages/engine/src`)

| 파일 | 역할 |
|---|---|
| `daniels.ts` | Daniels–Gilbert **공개 회귀식 2개**로 VDOT ↔ 기록 변환. 원 표의 상수는 리포에 없다 (O3) |
| `riegel.ts` | 거리 간 기록 환산 + 주간 거리 기반 지수 보정 |
| `fitness.ts` | 3가지 입력(대회기록 / 편한페이스 / 초보)을 VDOT + 주간거리로 정규화 |
| `gain.ts` | 주당 VDOT 향상률. 문헌 앵커 + 안전계수 0.5 의 연속 감쇠 모델 (O2) |
| `feasibility.ts` | 목표 타당성 판정 + **지구력 게이트** + 달성가능 기록 + 대안 목표 |
| `periodization.ts` | 페이즈 배분(base/build/peak/taper) + 볼륨 곡선 + **ACWR 1.30 하드 클램프** |
| `sessions.ts` | 주간 세션 배치. 존별 주간 거리 상한, 고강도 간격 제약 |
| `zones.ts` | E/M/T/I/R 페이스표. **M 은 고정 계수가 아니라 풀코스 예측시간에서 역산한다** |
| `plan.ts` | 진입점 `generatePlan(input) → Plan` |

### 저장소 (`packages/db`)

경계 하나만 기억하면 된다 — **이 패키지는 엔진을 모르고, 엔진은 이 패키지를 모른다.**
저장소는 `PlanInput` 을 JSON 으로 넣고 꺼낼 뿐 플랜을 만들지 않는다.
둘을 잇는 건 웹 어댑터(`apps/web/src/lib/my-races.ts`)다.

| 파일 | 역할 |
|---|---|
| `prisma/schema.prisma` | 테이블 셋 — `users` / `saved_plans` / `session_logs` |
| `src/client.ts` | Prisma 싱글턴. dev 서버 HMR 때 커넥션이 쌓이지 않게 `globalThis` 에 붙인다 |
| `src/users.ts` | 로그인 upsert · 해시 · 탈퇴(soft) · **하드 삭제 purge** |
| `src/plans.ts` | 저장 플랜 CRUD. 조회에 항상 `userId` 를 건다 |
| `src/logs.ts` | 수행 기록 (F-12). 저장소만 먼저 두고 화면은 나중 |
| `src/seed.ts` | 도그푸딩 플랜 한 건. **수행 기록은 만들지 않는다** |

`SavedPlan` 은 **플랜 본문이 아니라 `input` 만** 저장한다 (PRD §9.7).
엔진이 결정론적이라 입력만으로 완전 복원되고, `engineVersion` 이 그 복원의 기준선이다.

### 웹앱 (`apps/web/src`)

```
auth.ts          Auth.js 설정 (카카오 단독). 라우트를 막지 않는다 — §9.2
app/api/auth/    Auth.js 콜백. 이 리포의 유일한 route handler
app/(public)/    공개·SEO 라우트. 하단 탭 없음
app/(app)/       로그인 사용자 앱 셸. 하단 탭 있음
components/ui/   버튼·카드·스탯·빈상태 등 원자 컴포넌트
components/…/    home · layout · plan · log · races 도메인별
lib/             엔진 → 화면 어댑터, 포맷터, URL 인코딩, 상수
```

---

## 4. 아키텍처 결정과 그 이유

읽지 않고 뒤집으면 안 되는 것들.

### 4.1 엔진 분리 (PRD §11.2)
브라우저 없이 수천 케이스를 검증해야 하고, **빌드 타임에 같은 함수를 호출해 정적 SEO 페이지를 만들어야 한다.**
그래서 엔진은 React·Next 를 모르고, Node API 도 모른다.

### 4.2 결정론
같은 `PlanInput` → 항상 같은 `Plan`. 엔진 안에서 현재 시각을 읽지 않고 `today` 를 입력으로 받는 이유가 이것이다.
이 성질 위에 두 가지가 얹혀 있다.
- SSG (정적 대회 페이지)
- **URL 만으로 플랜 복원** — `/plan/result?p=<base64url>` 로 서버에 아무것도 저장하지 않는다

엔진 계수를 바꾸면 저장된 플랜이 달라진다. 그래서 `ENGINE_VERSION` 을 올리고 §9.7 고정 렌더링을 지킨다.

### 4.3 레이아웃 두 개
`(public)` 은 검색 유입·게스트 경로라 하단 탭이 없다. 크롤러와 첫 방문자에게 누를 수 없는 탭은 소음이다.
`(app)` 은 반복 방문하는 사용자용이라 탭바가 붙는다.

### 4.4 헤더는 레이아웃이 아니라 화면이 고른다
`(app)/layout.tsx` 는 `{children}` + `<TabBar/>` 만 그린다.
각 화면이 `AppScreen` 에 헤더를 넘긴다 — 탭 최상단은 `AppHeader`(로고), 한 단계 들어간 화면은 `SubHeader`(뒤로가기).
레이아웃에 헤더를 박아 두면 이 분기를 만들 자리가 없다.

### 4.5 저장 게이트 ≠ 생성 게이트 (PRD §9.2)
로그인은 **플랜 저장 시점에만** 요구한다. 생성은 게스트로 끝까지 된다.
로그인 벽을 앞에 세우면 핵심 지표(생성 완료율)가 죽는다 (R12).

### 4.6 SEO 대량생성 방어 (PRD §13.2)
대회 상세 정적 페이지는 82개 전부가 아니라 **고유 콘텐츠(코스·날씨 노트)가 있는 16개**(`seoReadyRaces`)만 만든다.
`/race` 목록 화면도 고유 콘텐츠 없는 대회는 상세로 보내지 않고 `/plan/new?race=` 로 직행시킨다.
템플릿에 숫자만 바꾼 페이지를 대량으로 뿌리면 사이트 전체가 저품질로 판정될 수 있다.

### 4.7 조용히 줄이지 않는다 (PRD §7.10)
ACWR 클램프나 존 상한이 발동해 플랜이 약해지면 `plan.notices` / `week.clamped` 로 **반드시 화면에 알린다.**

---

## 5. 라우트 지도

### 루트 `/` 가 두 갈래인 이유
`/` 는 **검색 유입의 착지점**이라 정적으로 사전 렌더된다 (질문형 H1 + 다가오는 대회 + 목표 클러스터).
`src/proxy.ts` 가 **세션 쿠키가 있을 때만** `/today` 로 보낸다.

```
크롤러 · 첫 방문자  →  / (정적 랜딩)  →  플랜 생성
로그인 사용자       →  / → /today (앱 셸)
홈 화면 앱          →  OS 스플래시 → /today   (manifest.ts 의 start_url)
```

**루트를 로그인 화면으로 바꾸지 말 것.** PRD §9.2 가 "콘텐츠를 로그인 벽 뒤로 넣는 것은
타협 불가 금지사항"으로 못 박아 뒀고, §13 SEO 전략 전체가 이 페이지에 걸려 있다.
`proxy.ts` 의 matcher 가 `/` 하나뿐인 것도 그래서다 — 다른 경로는 건드리지 않는다.

### 공개 `(public)` — 탭바 없음
| 라우트 | 렌더링 | 내용 |
|---|---|---|
| `/` | 정적 1h | 히어로 + 다가오는 대회 + 목표 클러스터 |
| `/race` | 정적 1h | **대회 달력**(닷 + 날짜 선택) + 그 아래 **전체 82개 목록**. 목록은 크롤러가 SSG 16개로 들어가는 링크 그래프라 지운다는 선택지가 없다 |
| `/race/[slug]` | SSG 16개 + 온디맨드 | 대회 상세. SEO 주 진입점, 구조화 데이터 |
| `/goal/[slug]` | SSG 13개 | 목표별 랜딩 (sub3, sub4 …). 문구는 목표마다 손으로 씀 |
| `/login` | 동적 | **로그인 전용 화면.** 카카오가 주, 이메일은 접어 둔다. `noindex` |
| `/plan/new` | 동적 | 입력 3스텝 위저드 |
| `/plan/verdict?p=` | 동적 | 타당성 판정 + 대안 |
| `/plan/result?p=` | 동적 | 페이스표 + 주차 아코디언 + 캘린더 내보내기 + 링크 복사 |
| `/tools/pace` `/tools/vdot` | 정적 1h | 페이스·VDOT 계산기. 문장은 `lib/tools.ts` 에서 손으로 쓴다 |
| `/privacy` `/terms` | 정적 | 법적 문서 |

### 앱 셸 `(app)` — 하단 탭 4개
| 라우트 | 탭 | 내용 |
|---|---|---|
| `/today` | 오늘 | 인사 + 오늘 세션 헤드라인 + NEXT RACE 카드 + 이번 주 |
| `/races` | 대회 | **내 대회** 목록 + "새로운 목표를 추가해보세요" 슬롯 |
| `/races/[slug]` | (대회 탭 유지) | **훈련 일정 상세** — 세션 카운터·오늘의 훈련·이번 주·전체 주차 |
| `/log` | 기록 | **월 캘린더**(완료·건너뜀·기록없음·계획·오늘·롱런) + 최근 러닝. 날짜를 눌러 지난 날도 체크한다. 목표가 여럿이면 `?plan=<key>` 선택기가 붙는다 |
| `/me` | 나 | 프로필 + 통계 3칸 + 설정 메뉴 |

> `/races/[slug]` 를 중첩 라우트로 둔 이유: 탭바가 `pathname.startsWith('/races/')` 로 활성 탭을 판정하므로
> 상세 화면에 들어가도 대회 탭이 켜져 있다. 사용자가 "어느 탭에 있는지"를 잃지 않는다.

### 게스트 흐름
```
/ 또는 /race 또는 /race/[slug]
        ↓
/plan/new          입력 3스텝 (뒤로가기가 이전 스텝)
        ↓
/plan/verdict?p=   판정 + 대안. 🔴이면 대안이 주 동선
        ↓
/plan/result?p=    페이스표 + 주차 아코디언 + 링크 복사
```

---

## 6. 디자인 시스템

토큰은 `apps/web/src/app/globals.css` 의 `@theme` **한 곳에만** 있다.
컴포넌트는 리터럴 색상값도, `text-[15px]` 같은 임의 크기도 쓰지 않는다.

### 6.1 활자 — 열한 단계 고정
한때 11~64px 사이 **19개 크기**가 화면마다 제각각 쓰였다. 15·16·17px 이 한 화면에 섞이면
위계가 아니라 흔들림으로 읽힌다. 그래서 스케일을 토큰으로 못 박았다.

| 토큰 | px | 쓰임 |
|---|---|---|
| `text-micro` | 11 | 탭 라벨·범례·고지 |
| `text-label` | 13 | 캡션·메타 |
| `text-body` | 15 | 보조 본문 |
| `text-body-lg` | 17 | 주 본문·목록 제목 |
| `text-section` | 19 | 섹션 제목 |
| `text-card` | 22 | 카드 제목 |
| `text-title` | 27 | 화면 제목 |
| `text-headline` | 33 | 오늘 화면 헤드라인 (화면당 하나) |
| `text-figure-sm` / `text-figure` / `text-hero` | 40 / 56 / 68 | 숫자 |

숫자에는 `.figure` 를 함께 건다 — tabular-nums + 자간 −0.035em + 800.

`word-break: keep-all` 이 body 에 걸려 있다. **한글을 어절 중간에서 끊지 않기 위해서다** —
기본값이면 '남은 기간에'가 '기간' / '에' 로 갈라진다. 긴 URL 만 `overflow-wrap` 으로 예외 처리한다.

### 6.2 색 — 역할별로 쪼갠다
톤: 따뜻한 크림 배경(`canvas #fbf7f0`) + 보라 액센트. 숫자는 크고 굵게, 나머지는 조용하게.

- **하나의 보라로 채움과 글자를 겸하면 반드시 한쪽이 깨진다.** 그래서 역할별로 나눠 뒀다.
  `brand`(#7c5fe0) 버튼·진행바·체크 채움 · `brand-ink`(#6c53c3) 크림 위 글자 · `on-brand` 보라 채움 위 글자.
- **보라는 아껴 쓴다.** 목업에서 보라인 것은 넷뿐이다 — 섹션 라벨, 주 버튼, 진행바, 활성 탭.
  카드를 보라로 채우면 D-day 가 배경색과 경쟁해서 오히려 덜 읽힌다.
- `ink-subtle`·`brand-line` 은 장식 전용이라 **텍스트 금지**.
- 표면 네 단계: `surface-sunken` → `canvas` → `surface` → `surface-raised`.

### 6.3 카드 톤 = 화면의 위계
`<Card tone>` 은 세 가지다. 위계는 **색이 아니라 떠 있는 정도**로 만든다.

| tone | 쓰임 |
|---|---|
| `plain` | 기본 |
| `raised` | 지금 봐야 할 것 하나 (흰 면 + 그림자) |
| `sunken` | 가라앉은 보조 정보 |

`raised` 는 화면당 하나로 제한한다 — `/today` 의 NEXT RACE, `/races/[slug]` 의 오늘의 훈련.
같은 화면에서 두 번 쓰면 둘 다 주인공이 아니게 된다.

### 6.4 지키는 규칙
- `npm run contrast` 가 토큰별 명도 대비를 WCAG 기준과 대조하고 미달이면 **빌드처럼 실패한다.**
  새 색을 추가하고 계약(`CONTRACT`/`DECORATIVE`)에 등록하지 않으면 **미등록으로 걸려서 실패한다** —
  대비를 정하지 않았다는 사실 자체를 잊지 않게 하는 장치다.
- **화면 구성의 기준은 `docs/mockups/` 다.** 코드가 목업과 다르면 둘 중 하나를 고치되
  말없이 어긋난 채로 두지 않는다. 다만 목업이 PRD·접근성과 부딪히는 자리는 §8 표에 남긴다.
- **색만으로 뜻을 전하지 않는다** (WCAG 1.4.1). 판정 배지는 이모지+한글 라벨이 함께 가고,
  캘린더 마커는 빈 원 / 체크 원 / 겹친 원 / 채운 원으로 **형태가 갈리며**,
  탭바 활성 탭은 색과 함께 선 굵기·라벨 굵기가 같이 바뀐다.
- 달력은 **월요일 시작**이다. 엔진이 주차를 월요일부터 끊으므로(`week.startDate`),
  일요일 시작이면 한 훈련 주차가 두 줄에 걸쳐 보여서 주간 볼륨이 읽히지 않는다.
- 터치 타깃 최소 44px (`--spacing-touch`). 화면 좌우 여백은 `--spacing-gutter`(20px) 하나로 통일.
- 섹션 간격은 화면이 고르지 않는다. `<Screen>`/`<Section>`(`components/ui/section.tsx`)이 리듬을 쥔다.
- 모션은 `.pressable`(눌림)과 `.rise`(진입) 둘뿐. 둘 다 `prefers-reduced-motion` 에서 꺼진다.
- 일러스트는 `lib/illustrations.ts` 슬롯 레지스트리. 화면 컴포넌트는 파일 이름을 모른다.
  그림을 바꾸려면 `src` 만 고친다. `ratio` 는 CLS 방어용이다 (PRD §10.9).

### 6.5 아트워크를 쓰는 법
`public/illustrations/scene-*.png` 10장은 전부 **3:2 가로 씬이고 위쪽 60%가 빈 하늘**이다.
배경색도 크림 캔버스와 거의 같다. 구석에 작게 오려 붙이라고 만든 그림이 아니라
**글자를 얹으라고 만든 그림**이다. 그래서 쓰는 방법이 셋뿐이다 (`components/ui/scene.tsx`).

| 컴포넌트 | 쓰임 |
|---|---|
| `SceneSurface` | 씬을 면으로 깔고 그 위(빈 하늘)에 글자를 얹는다. 대회 카드·오늘의 훈련 |
| `SceneBand` | 그림만 한 줄. **`aspect` 를 원본보다 납작하게 줘서 하늘을 잘라 낸다** — 안 그러면 빈 크림만 크게 보인다 |
| `SceneAvatar` | 원형 크롭. 초점은 오른쪽 아래(언덕·해)다 |

밟았던 함정 셋:
- **버튼·진행바를 씬 위에 반투명으로 얹지 말 것.** 씬의 아래쪽(러너와 길)을 정확히 덮어서
  그림이 있으나 마나 해진다. 흰 바닥을 그림 **밖**에 따로 둔다.
- **음수 z-index 금지.** 카드 배경(`bg-surface-raised`) 뒤로 숨어서 그림이 사라진다.
  절대 배치된 이미지는 컨테이너 배경 위, 뒤따르는 `relative` 자식 아래에 그려진다 — 순서로 충분하다.
- **`<Image width height>` 로 정사각을 못 만든다.** Tailwind preflight 의 `img { height: auto }` 가
  height 속성을 이겨서 원본 비율로 눕는다. 크기는 바깥 상자가 잡고 이미지는 `fill` 로 채운다.

씬 파일은 **1200px WebP** 다 (`npm run art` 가 만든다). 납품 원본은 2304×1536 PNG 로
장당 4~5MB, 20장 88MB 였는데 1.3MB 가 됐다 — 앱이 쓰는 최대 슬롯 폭이 448px 이라
화질 손해는 없다. **원본을 새로 받으면 `npm run art` 를 돌린 뒤 커밋할 것** (O18 해소).

---

## 7. 검증 절차

작업을 끝냈다고 말하기 전에 전부 통과시킨다.

```bash
npm run db:up                       # 로컬 Postgres (Docker). 앱 셸 화면이 이걸 읽는다
npm test                            # 엔진·대회 데이터·통합 (133개)
pnpm --filter @runback/web test    # 웹 유닛 (60개)
npm run typecheck                   # 엔진·순수성·대회·저장소·루트
npm run contrast                    # 명도 대비
npm run legal                       # 법적 고지 — 플레이스홀더·이메일 수집 일치·국외 이전
# 파기 Cron 수동 실행: curl -H "Authorization: Bearer $CRON_SECRET" localhost:3000/api/cron/purge
pnpm --filter @runback/web build   # 프로덕션 빌드 + 타입체크
npm run e2e                         # Playwright E2E 141개 (docs/qa-sheet.md 의 자동화분)
pnpm --filter @runback/web dev     # 로컬 확인 → http://localhost:3000/today
```

기준선 (2026-09-11): 테스트 **193개 전부 통과**(엔진·통합 133 + 웹 60), **E2E 141개 전부 통과**
(mobile 64 + desktop 64 + account 13 — account 는 DB 가 없으면 skip),
대비 **전 항목 통과**, 정적 페이지 **44개**.

### E2E (`e2e/`, `playwright.config.ts`)
`docs/qa-sheet.md` 의 자동화 가능한 항목을 옮긴 것이다. **프로덕션 빌드**를 3100 포트에 띄워서 돈다 —
dev 서버는 정적/SSG 판정이 달라 §13 항목을 제대로 못 본다. 3000 과 `.next` 는 사람 몫이라 안 건드린다.

| 프로젝트 | 무엇 |
|---|---|
| `mobile` / `desktop` | 게스트 흐름·판정·SEO·접근성. 뷰포트 두 벌 |
| `account` | 계정·앱 셸. **한 번만** 돈다 — 같은 계정에 동시 로그인하면 scrypt 비용과 실패 잠금이 겹쳐 흔들린다 |

계정 테스트는 `e2e/global-setup.ts` 가 만드는 `e2e@runback.kr` 을 쓴다. **DB 가 없으면 실패가 아니라 skip 이다** —
게스트 경로는 DB 없이도 전부 돌아야 하므로(§9.5) DB 부재를 실패로 만들면 그 설계를 테스트가 부정하게 된다.

### DB 를 처음 띄울 때
```bash
cp .env.example .env                # DATABASE_URL·AUTH_SECRET·AUTH_ID_PEPPER
npm run db:up                       # Postgres 17 (docker compose)
npm run db:migrate                  # 마이그레이션 적용
pnpm --filter @runback/db user:create -- test@runback.kr '비밀번호10자이상'
npm run db:seed                     # 도그푸딩 플랜을 그 계정에 붙인다
```

로그인은 `/me` 에서 한다. 카카오 키가 없어도 **이메일+비밀번호**로 들어갈 수 있다
(`AUTH_PASSWORD_LOGIN=true`). 게스트 경로를 보려면 로그아웃하면 된다 —
예전에 있던 `RUNBACK_DEV_USER_ID` 우회로는 걷어냈다. 개발 중에 늘 로그인 상태로 보여서
**게스트 흐름을 테스트할 수 없었기 때문이다.**
`npm run db:studio` 로 데이터를 눈으로 본다. 스키마를 갈아엎을 땐 `docker compose down -v`.

### dev 서버를 두 대 띄울 때
Next 16 은 프로젝트당 dev 서버를 **하나로 제한한다** (`.next/dev/lock`). 사람이 3000 을 켜 둔 채
에이전트가 따로 검증하려면 빌드 디렉터리를 갈라 준다.

```bash
NEXT_DIST_DIR=.next-agent pnpm --filter @runback/web dev --port 3100
```

`next.config.ts` 의 `distDir` 이 이 환경변수를 읽는다. 기본값은 `.next` 라 평소엔 영향이 없다.

**DB 가 꺼져 있으면** 앱 셸 4개 탭은 빈 상태로 그려진다 — 게스트 경로(`/`, `/race`, `/plan/*`)는
DB 를 전혀 쓰지 않으므로 그대로 동작한다. 이건 버그가 아니라 §9.5 설계다:
**계정은 편의 레이어이지 단일 진실 공급원이 아니다.**

보조 명령: `npm run brand`(브랜드 자산 재출력) · `npm run art`(일러스트 재출력) · `npm run table`(페이스표) · `npm run gain`(향상률 모델) · `npm run plan`(플랜 한 건 출력) · `npm run races`(대회 데이터 점검)

### §11.3 불변식
`packages/engine/test/_invariants.ts` 가 PRD §11.3 을 그대로 검사한다.
거리 4종 × 훈련일수 4종 × 주차 1~52 전수 스윕 + 무작위 2,000건.
**여기 통과하지 못하면 배포하지 않는다.** 엔진 계수를 건드렸으면 이 테스트부터 돌린다.

---

## 8. 현재 상태

| 주차 | 범위 | 상태 |
|---|---|---|
| W1 | VDOT 코어·Riegel·페이스표 | ✅ ADR-0001 로 O3 해소 |
| W2 | 판정·페이즈·ACWR·세션 배치 | ✅ ADR-0002 로 O2 해소. §11.3 전 항목 통과 |
| W3 | 대회 데이터 82개 + 입력 스텝 | ✅ |
| W4 | 판정 화면 + 결과 뷰 + 페이스표 | ✅ **게스트 전 흐름 완주** |
| W5 | SEO 마감 + 방침 문서 | ✅ **도메인 `runback.kr` 확정으로 O1' 해소.** 남은 건 시행일(인증 켜는 날) |
| — | 앱 셸 4개 탭 화면 | ✅ 2026-09-09 완료 (아래) |
| — | **BE Phase 1 — 데이터 계층** | ✅ 2026-09-10 완료 (아래) |
| W6 | **인증 F-16** | ✅ 2026-09-10 — 카카오 로그인 배선 완료. **카카오 앱 키만 넣으면 동작** |
| W6 | **저장 F-17 + 게스트→회원 전환** | ✅ 2026-09-10 |
| W6 | **탈퇴 F-18 + 내보내기 F-19** | ✅ 2026-09-10 — 파기 Cron 포함. **법적 릴리즈 조건 충족** |
| W7 | **수행 체크 F-12** | ✅ 2026-09-10 — O17 종결. 진행률·캘린더·최근 러닝이 진짜가 됐다 |
| W7 | 게스트→회원 이전 계측 · O16 다중 목표 | ⬜ |
| W7 | 수행 체크(F-12) + 게스트→회원 이전 | ⬜ 미착수 |

### 2026-09-10 에 한 것 — 도메인 확정 + 백엔드 데이터 계층

**Phase 0 — 결정 (전부 종결)**

| 항목 | 확정 |
|---|---|
| 도메인 | **runback.kr** → `SITE_URL`, `privacy@runback.kr` (O1' 종결) |
| DB | 로컬 Docker Postgres + 프로덕션 Neon. 같은 Postgres, `DATABASE_URL` 만 다르다 |
| ORM | **Prisma** — 스키마가 3테이블이라 ORM 간 차이가 드러나지 않는다. 기준은 "6개월 뒤 읽을 수 있는가" |
| 인증 | **카카오 단독.** 애플은 유료 개발자 계정이 필요해 보류 (O12 종결) |
| 탈퇴 | soft delete **15일** → 하드 삭제 (O11 종결) |

**Phase 1 — 데이터 계층**
- `packages/db` 신설. Prisma 스키마 3테이블 + 마이그레이션 2개
- `docker-compose.yml` (Postgres 17) + `.env.example`
- `lib/demo-plan.ts` **삭제** → `lib/my-races.ts` 가 DB 에서 읽는다. 앱 셸 5개 화면 전환 완료
- `lib/session.ts` — 현재 사용자 한 곳. **Phase 2 는 이 파일만 고치면 된다**
- `lib/plan-input.ts` — 임의 값을 `PlanInput` 으로 확인. URL 과 DB 두 경로가 같은 경계값을 쓴다
- 패키지 스코프 `@raceback` → `@runback` (제품명과 어긋나 있던 마지막 표기)

**방침에 반영한 것** — 결정이 바뀌면 방침도 바뀐다. 어긋나면 그게 곧 위반이다.
- **국외 이전 조항 신설** (제7항). Vercel·Neon 이 국외 사업자라 법 제28조의8 고지 의무가 생겼다
- 보유기간·파기절차에 **15일** 명시, 위탁에서 Apple 제거
- 항 번호 7~13 → 8~14 로 밀고 교차 참조(제9항·제10항) 정정

### 2026-09-11 에 한 것 — 입문자 세션 수행 가이드 (§7.8-1)

"러닝을 처음 시작하는 사람에게 훈련 방법 가이드가 있나"에서 출발했다. 찾아보니
**적응은 꽤 들어가 있는데 설명이 없었다** — 입력 경로 ③(`novice`), 존 축소(E·M),
지구력 게이트, 보수적 판정까지 전부 **숫자를 맞춰 주는** 쪽이고, 정작 그 숫자를 받은
사람이 무엇을 해야 하는지는 아무 데도 없었다. 화면이 주는 건 `EASY RUN 2.8km · 7:37~8:39` 뿐이다.

**§7.8 을 화면이 지키다가 `structure` 에서 새고 있었다.** 입문자에게 존을 E·M 으로
접어 놓고 정작 첫 주 첫 세션 설명에 **`이지런 후 스트라이드 20초 × 4`** 가 그대로 나온다.
스트라이드는 R존 개념이다 — 배지는 가리고 용어는 통과시키니 읽고도 모르는 상태였다.
가이드가 메우는 실제 구멍이 이것이다.

**붙인 자리는 둘.** 플랜 전체(`/plan/result` 페이스표 다음)와 오늘 하나
(`/today`·`/races/[slug]` 훈련 카드 아래). 주차 아코디언의 줄마다 붙이지 않는다 —
같은 설명이 수십 번 반복되면 플랜 자체가 안 읽힌다. **종류당 한 번만** 말한다.

**노출 기준을 §7.8 과 같은 한 줄로 묶었다** (`planLevel()`). 화면마다
`fitness.kind === 'novice'` 를 따로 적으면 한쪽만 바뀌는 날이 오고, 그러면 한 제품 안에서
'입문자'의 정의가 둘이 된다. 페이스표도 이제 이 함수를 쓴다.

**가이드는 숫자를 한 줄도 쓰지 않는다.** 거리·페이스·반복 횟수는 전부 엔진 몫이라
여기에 "20분 정도"가 들어가면 엔진 값과 어긋나고, 어긋나면 어느 쪽을 믿어야 할지 모른다.
말하는 것은 **강도의 느낌과 판단 기준**뿐이다 — *"옆 사람과 대화가 되는 속도"*,
*"마지막 반복이 첫 반복과 비슷하게 끝나야 한다"*. 통증·부상은 다루지 않는다 (§7.10).

**이 계약을 문장이 아니라 테스트로 박았다.** 숫자 금지·의학 표현 금지·위협 표현 금지를
화면에 나가는 모든 문장에 대해 검사한다. 지키자고 적어 둔 규칙은 언젠가 깨진다.

**용어 사전은 `structure` 에 실제로 뜬 것만 고른다** (`guideTermsIn`). 통째로 펼치면
그건 가이드가 아니라 사전이다. ⚠️ **엔진이 `structure` 문구를 바꾸면 사전이 조용히
어긋난다** — 화면에는 설명이 안 보일 뿐이라 아무도 모른다. 그래서 테스트가 실제 플랜에서
뽑은 structure 로 양방향을 검사한다(사전이 못 잡는 말 / 엔진이 더 이상 안 쓰는 말).

> **테스트 스윕을 좁게 잡았다가 한 번 속았다.** `canRunMin` 20 하나에 기간 3종으로 돌리니
> '크루즈 인터벌'이 한 번도 안 나와서 **사전에 죽은 항목이 있다**고 잘못 판정했다.
> 실제로는 입문자도 주 5~6일 장기 플랜에서 만난다 (560케이스 중 1311건). 세션 구성은
> 주간 거리에서 갈리므로 **거리를 키우는 축(훈련일수·기간)을 넓게** 잡아야 한다.

테스트 47 → 60, E2E 131 → 141. E2E 는 유닛이 못 보는 것만 본다 — **화면에 실제로 떴는가**,
**떠서는 안 되는 사람에게 안 떴는가**. 조건부 렌더는 조용히 사라져도 티가 안 난다.

⚠️ **앱 셸 두 화면(`/today`·`/races/[slug]`)은 브라우저로 확인하지 못했다.** Docker 가
꺼져 있어 DB 가 없고, 계정 E2E 13개가 skip 된다. 빌드·타입체크는 통과했고 같은 컴포넌트를
`/plan/result` 에서 눈으로 확인했지만, **실제 클릭은 안 밟았다** (§11 의 그 함정이다).

### 2026-09-11 에 한 것 — 대회 일정을 달력으로 (`/race`)

"목록이 한도 끝도 없이 늘어진다"에서 출발했다. 데이터를 세어 보니 방향이 맞았다 —
**82개 / 지평선 191일인데 대회가 있는 날은 27일뿐**이고, 2026년 10월에 38개가 몰려 있다.
날짜순 목록은 그 밀도를 통째로 숨긴다.

**두 겹으로 만들었다.** 위는 달력(사람), 아래는 월별 전체 목록(크롤러).
달력만 두면 한 번에 한 달치 링크만 HTML 에 남아 **§13.1 SSG 16개로 가는 길이 끊긴다.**
확인: `<main>` 안에 상세 링크 16개·플랜 직행 66개 = 82개가 그대로 있다. JS 없이도 동작한다.

**기본 목록을 'N일 이내'로 하지 않은 이유.** 처음 제안은 7일이었는데 둘이 걸렸다.
① 12~1월엔 7일 창이 몇 주 내내 비어서, 82개를 들고 있으면서 빈 화면을 띄우게 된다.
② **7일 남은 대회는 §7.3 최소 권장(하프 10주·풀 12주)을 한참 밑돌아 판정이 거의 다 🔴 다.**
이 제품은 10~12주를 역산하는 도구인데 기본 화면이 정작 **행동할 수 없는 대회**만 보여 주게 된다.
→ 날짜를 안 고르면 **지금 보는 달 전체**. 달력과 목록이 같은 것을 가리켜 관계가 바로 읽힌다.

**막다른 길을 두 군데서 막았다.**
- 월 이동을 **데이터 지평선 안으로 제한** (오늘의 달 ~ 마지막 대회의 달). 안 막으면 목록보다 더 심한 '한도 끝도 없음'이 된다
- 지평선 **안**의 빈 달(2027년 1월)에는 "2027년 2월로 이동 →" 을 준다

**달력 뼈대를 `lib/month-grid.ts` 로 뺐다.** 훈련 달력(`/log`)과 대회 달력이 같은 규칙
(월요일 시작·앞뒤 주 채우기)을 쓰는데 두 벌이면 한쪽만 고쳐진다 — 같은 제품 안에서
**달력 두 개가 다른 요일에서 시작하는** 일이 생긴다. `monthGrid()` 도 이걸 쓰도록 고쳤다.

**같이 정리한 것 둘** (작성자 지적).
- **월 이름을 한글로.** 목업이 `September 2026` 이라 `/log` 가 그걸 따르고 있었고 `/race` 도
  물려받았는데, **요일은 이미 한글**이라 한 달력 안에서 섞여 있었다. 두 달력 다 `2026년 9월` 로 맞췄다
- **전체 목록을 한 줄짜리 색인으로.** 달력 밑 목록과 같은 3줄 카드로 그리니 같은 것이 두 번 보여서
  어느 쪽이 주인공인지 안 읽혔다. `날짜 · 이름 · 지역 · D-day` 한 줄로 줄여 **모양으로 '색인'임을 알린다.**
  링크 82개는 그대로다. 종목(5K·10K·하프)은 뺐다 — 82번 반복되는 일반 명사라 색인에서 하는 일이 없다

E2E 케이스 8개 추가 — 닷·선택·해제·지평선 경계·빈 달 탈출(`race-calendar` 7건) +
JS 없는 링크 그래프(`seo-tools` 1건). **뷰포트 두 벌이라 실행 수는 16개 늘어 115 → 131개**다.

### 2026-09-11 에 한 것 — Playwright E2E (QA 시트 자동화)

`docs/qa-sheet.md` 를 사람이 매번 손으로 훑는 문서로 두지 않으려고 자동화했다. **115개 통과.**

**프로덕션 빌드를 대상으로 돈다.** dev 서버는 정적/SSG 판정이 다르고 첫 요청에 컴파일이 끼어들어
§13 항목(사전 렌더 여부·sitemap·JS 없는 상태)을 제대로 못 본다.

**엔진 결과를 테스트가 다시 계산하지 않는다.** 그러면 엔진의 버그를 그대로 베껴 쓴다.
판정 경계의 정확도는 `npm test` 가 보고, E2E 는 **화면이 그 결과를 제대로 보여 주는가**만 본다.

**E2E 가 잡아낸 것 셋**

**🐛 ① 공개 페이지 본문이 `<main>` 밖에 있었다 — SEO 전략이 걸린 자리다.**
`(public)/loading.tsx` 가 라우트 그룹 전체에 Suspense 경계를 만들어서, 정적 페이지인데도
본문이 `<div hidden>` 으로 스트리밍되고 JS 가 그걸 옮겨 넣고 있었다.
`<main>` 안에는 스켈레톤 2.6KB 뿐이었다 — `/race` 도, `/goal/*` 도, 계산기도 전부.
**§13.3 의 FAQPage JSON-LD 까지 hidden 안에 있었다.** 구글봇은 JS 를 돌리지만
PRD 가 겨냥하는 AEO/GEO 쪽 수집기 상당수는 그러지 않는다.
→ `loading.tsx` 를 `(public)/plan/` 으로 내렸다. 스켈레톤이 실제로 필요한 건 동적 라우트고,
정적 SEO 페이지는 기다릴 것이 없다. `/race` 의 `<main>` 이 2.6KB → **42KB** 가 됐다.

**🐛 ② 터치 타깃 미달** — 공개 헤더(28px·24px)와 푸터 링크(20px)가 `--spacing-touch`(44px)를
못 지키고 있었다. 화면마다 붙는 것이라 **모든 공개 화면**이 미달이었다.

**🐛 ③ 프로덕션 http 에서 로그인이 안 붙었다** (E2E 환경 한정, 배포에는 영향 없음).
Auth.js 가 프로덕션에서 `__Secure-` 쿠키를 쓰는데 브라우저가 http 오리진에서 버린다.
서버 로그에는 성공으로 남고 화면만 게스트라 원인이 잘 안 보인다.

**밟은 함정** — 이모지 문자 클래스 `[🟢🟡🔴]` 는 `u` 플래그 없이 쓰면 서로게이트 쌍이
반쪽으로 쪼개져 **아무것도 안 잡힌다.** 판정 배지를 못 읽어 20개가 한꺼번에 실패했다.

### 2026-09-11 에 한 것 — PRD↔구현 점검 · 🔴 고리 · 계산기 2종

**① PRD 문서를 구현에 맞췄다** — §10.8 계정 화면(`/races`·`/me`), F-17 경로, §9.10, §13.1 미구현 표기.

**② §15 이벤트 3개를 배선하고 브라우저로 실제 발화를 확인했다.**
`fitness_input`(위저드 2→3 스텝) · `goal_adjusted`(🔴 대안 클릭) · `login_dismissed`(비로그인 링크 복사).
정적 점검으로는 13개 전부 O 였지만 **정적 점검은 "코드가 있다"까지만 말한다.**

> ⚠️ 확인할 때 두 번 헛다리를 짚었다. `dataLayer` 항목은 배열이 아니라 **`arguments` 객체**라
> `Array.isArray` 로 거르면 전부 사라지고, `TrackEvent` 는 이펙트에서 쏘므로 **탐색이 마운트를
> 앞지를 수 있다.** 둘 다 "이벤트가 하나도 안 나간다"로 보인다 — 실제로는 잘 나가고 있었다.

**③ 🐛 🔴 판정 화면에 빠져나갈 수 없는 고리가 있었다.**
화면이 `req.input.goal.kind`(사용자가 적어 낸 목표)로 갈렸는데, 풀코스 기간 미달이면
엔진이 §7.3 에서 기록 목표를 **완주로 바꿔 버린다**. 그래서 같은 화면이
"완주 목표로 전환했습니다"라고 말해 놓고 바로 아래에서 "4:28:35 를 노려보라"고 권했고,
그 대안을 누르면 **목표만 바뀐 채 같은 🔴 로 돌아왔다.**
§10.4 는 v1.5 에 이미 "완주 목표면 대안 기록을 그리지 않는다"고 적어 뒀는데
**기준으로 삼은 값이 틀렸던 것**이다. 지금은 `plan.feasibility.effectiveGoal.kind` 로 갈린다.

**④ 계산기 2종 (§13.1 `/tools/*`)**
- `/tools/pace` — 목표 기록 ↔ 페이스. 핵심은 평균 페이스가 아니라 **구간 통과 시간표**다.
  레이스에서 시계를 보고 판단하는 단위가 그것이다
- `/tools/vdot` — 기록 하나 → VDOT · 다른 거리 예상 기록 · **훈련 존 페이스표**. 셋째가 실제 쓸모다

**계수를 한 줄도 새로 쓰지 않았다.** 전부 엔진(`vdotFromRace`·`predictRaceTimeSec`·`paceTable`)이
낸다 — 엔진이 런타임 의존성 0인 순수 함수라 브라우저에서 그대로 돈다 (§4.1).
문장은 `lib/tools.ts` 에 도구별로 손으로 쓴다 (§13.2 규칙 3).

**§13.1 의 나머지 2종(대회×종목·대회×목표 ~250페이지)은 보류다 (O20).**
지금 만들면 템플릿에 숫자만 바꾼 페이지가 되고, 그게 §13.2 가 경고하는 바로 그 형태다.

### 2026-09-10 에 한 것 — 파비콘 시인성

"크롬 탭에서 R 아이콘이 잘 안 보인다"에서 출발했는데 **두 가지가 겹쳐 있었다.**

**🐛 ① 파비콘에 검은 배경이 구워져 있었다 — 내가 만든 버그다.**
`build-brand-assets.ts` 가 투명을 유지해야 할 아이콘에도 `flatten()` 을 불렀다.
**알파 0 인 배경을 넘겨도 `flatten()` 은 알파 채널 자체를 없앤다** — 그래서 마크가
검정 바닥 위에 얹혔다. 원본은 93.9% 투명인데 결과물은 투명 픽셀이 **0개**였다.

**② 고쳐도 다크 탭에서는 여전히 안 보인다.**
브랜드 보라 rgb(105,42,219) 는 어두워서 크롬 다크 탭(rgb 32~53) 대비가 **1.7:1** 이다
(WCAG 비텍스트 기준 3:1). 라이트 탭에서는 7.3:1 로 멀쩡하다 — 하나의 색으로는 양쪽을
만족시킬 수 없다.

**테마별로 파일을 나눴다.** `media="(prefers-color-scheme: dark)"` 로 갈린다.

| | 라이트 탭 | 다크 탭 |
|---|---|---|
| `favicon-*.png` | 7.31 / 5.58 : 1 | — |
| `favicon-*-dark.png` | — | 4.28 / 5.71 : 1 |

밝기 배수 1.85 는 눈으로 고른 게 아니라 **재서 정한 값**이다. 색을 바꾸면
`npm run brand` 뒤에 대비를 다시 잴 것.

⚠️ `<link rel="icon">` 에서 **다크 항목이 먼저 와야 한다.** 조건 없는 항목이 앞에 오면
일부 브라우저가 그걸 먼저 채택하고 `media` 항목을 무시한다.

### 2026-09-10 에 한 것 — 🔴 판정 화면 동선 (§10.4)

"완주 목표로 바꾼 뒤에 플랜을 보러 가려면 어디를 눌러야 할지 모르겠다"에서 출발했다.
파 보니 **완주 목표 + 🔴 화면이 세 가지로 어긋나 있었다.**

**🐛 ① 이미 완주인데 "완주 목표로 바꾸기" 버튼이 그대로 있었다.** 눌러도 제자리다.

**🐛 ② "그래도 **원래 목표로** 플랜 보기" 문구가 고정이었다.** 완주로 바꾼 뒤에는
그게 이미 '원래 목표'가 아니라 문구가 사실과 달랐다. 지금은 목표 종류에 따라 갈린다.

**🐛 ③ "완주가 어렵다"고 해 놓고 "현실적으로 노려볼 기록 2:32"를 제안했다.**
대안 기록은 VDOT 예측이라 지구력을 안 본다. 완주가 안 된다고 말한 화면에서 기록을 권하면
사용자는 무엇을 믿어야 할지 모른다. 완주 목표일 때는 이 블록을 아예 그리지 않는다.

**고친 방식 — 목표 종류에 따라 "다음에 할 일"이 하나로 읽히게 한다.**

| 상황 | 권장 동선 | 빠져나가는 길 |
|---|---|---|
| 🔴 + 기록 목표 | 대안 기록 카드 → **완주 목표로 바꾸기** | 그래도 **이 목표로** 플랜 보기 |
| 🔴 + 완주 목표 | 무엇을 바꿀 수 있는지 안내 → **입력 바꿔서 다시 보기** | 그래도 **완주 목표로** 플랜 보기 |

완주 목표에서는 더 낮출 목표가 없다 — 바꿀 수 있는 건 대회·기간·훈련 일수뿐이라
그게 주 동선이다.

**빠져나가는 길을 밑줄 링크에서 버튼(ghost)으로 바꿨다.** §7.3 의 '생성 차단' 취지를 지키려고
주 버튼으로 두지 않는 건 맞지만, 밑줄 텍스트 하나만 있으면 **어디를 눌러야 다음으로 가는지가
읽히지 않는다.** 무게는 낮추되 누를 수 있다는 건 보이게 한다. 대신
"플랜은 만들어지지만 권장하지 않습니다"를 붙여 말없이 통과시키지 않는다 (§7.10).

### 2026-09-10 에 한 것 — 캘린더 내보내기 (F-11)

PRD 가 P1 중 유일하게 "우선 검토"로 지목한 항목이다. **캘린더는 사용자가 앱을 열지 않아도
먼저 말을 건다** — North Star 가 D+7 재방문(§3.2 H1)인 제품에서 그게 핵심이다. O5 종결.

`/api/calendar` **하나**로 게스트(`?p=`)와 회원(`?plan=`)이 모두 받는다.
엔진이 결정론적이라 둘 다 같은 결과가 나온다. 게스트 경로를 남기는 이유는 §9.2 — 로그인 벽 없이.

**라이브러리를 쓰지 않는다.** iCalendar 는 텍스트 포맷이고 쓰는 범위가 좁다.
대신 RFC 5545 의 까다로운 것들을 정확히 지킨다 — 이게 이 작업의 실체다.

| 규칙 | 왜 |
|---|---|
| 줄 접기 **75 옥텟** | 글자 수가 아니라 **바이트**다. 한글은 3바이트라 글자로 세면 캘린더가 줄을 잘못 읽는다. 멀티바이트 문자 중간에서 자르면 깨진 바이트가 나온다 |
| **CRLF** 줄바꿈 | LF 만 쓰면 파일 전체를 거부하는 캘린더가 있다 |
| `\` → `\;` → `\,` 순서 | 백슬래시를 **먼저** 이스케이프하지 않으면 우리가 넣은 이스케이프를 또 이스케이프한다 |
| `DTEND` = 다음 날 | 종일 일정은 끝이 열린 구간이다 |
| `UID` = 날짜 + 플랜 키 | 같은 플랜을 다시 받아도 **일정이 늘지 않고 갱신된다** |
| `DTSTAMP` = 플랜 생성일 | '지금'으로 찍으면 받을 때마다 파일이 달라져 결정론(§4.2)이 깨진다 |

**알람(VALARM)을 넣지 않았다.** 재방문 유도에는 분명 도움이 되지만 **남의 캘린더에 알림을
심는 일**이고, 원치 않는 알림은 캘린더를 통째로 지우게 만든다. 캘린더 앱의 자체 설정에 맡긴다.

**`<a download>` 을 쓰지 않는다.** iOS 는 `.ics` 를 다운로드가 아니라 **캘린더 앱으로** 넘겨야
하는데 `download` 속성이 그 동작을 막는다. 서버의 `Content-Disposition` 이 그 일을 한다.

테스트 34 → 47. 캘린더 앱은 틀리면 **조용히 거부하거나 일정을 중복 생성**하므로 포맷을 못 박았다.

### 2026-09-10 에 한 것 — 판정 로직 전면 수정 (§7.3)

"기간이 짧아도 실력이 되면 비현실적이 나오면 안 된다"는 지적에서 출발했는데, 파 보니 **결함이 셋**이었다. 모두 **달력·볼륨 규칙이 실력을 덮어쓰는** 같은 모양이다.

**🐛 ① 완주 목표 판정이 실력을 아예 안 봤다.**
`weeksAvailable >= minWeeks * 0.6` 이라는 달력 규칙 하나였다. 그래서 주 80km 를 뛰는 사람과
주 10km 를 뛰는 사람이 **똑같이 🔴** 이었다. 더 나쁜 건 역전이다 — 같은 사람이 기록 목표를
고르면 🟡 인데 **더 쉬운 완주 목표를 고르면 🔴** 이 됐다.

**🐛 ② §7.5 규칙 5 를 잘못 구현했다.**
PRD 는 "계산된 피크가 **목표 달성에 필요한 수준**에 못 미치면"이라고 적었는데 코드는
**거리별 이상적 피크**와 비교했다. 그래서 하프 1:38 을 뛰는 사람이 1:45 목표로 5주를 잡으면
"권장 볼륨 미달"을 이유로 🔴 이 됐다.

**🐛 ③ 🔴 경계에 완충이 없었다.**
`gap > capacity` 가 절벽이라 0.1% 초과와 300% 초과가 같은 판정이었다.
하프 1:50 러너가 14주에 1:45 를 노리면 달성가능 예측이 **1:45:03** — **3초 차이로 🔴**.
capacity 는 PRD 스스로 "가설"이라 적은 향상률에 안전계수 0.5 를 곱한 보수적 추정이다.
그걸 3초 넘겼다고 "불가능"이라 하면 모델보다 판정이 더 단정적이다.

**고친 방식 — 지구력 게이트 (§7.3 신설)**

하프·풀 완주를 가르는 건 VDOT 가 아니라 **주간 거리**다. 그래서 완주 판정을 거기서 낸다.

| 거리 | 필요 주간 거리 |
|---|---|
| 5K / 10K / Half / Full | 15 / 20 / 30 / 45 km |

**증가율 상수를 지어내지 않았다.** §7.5 볼륨 곡선이 이미 ACWR 1.30 클램프와 감량 주차를
반영하므로, **그 곡선이 실제로 도달할 피크**를 그대로 쓴다. 별도 상수를 두면 곡선이 바뀔 때
판정만 조용히 어긋난다. (`volumeFactor` 는 1 로 둔다 — 판정 결과로 볼륨을 줄이는 건
이 판정 *다음* 일이라 넣으면 순환이 된다.)

최종 판정은 **3단계 판정과 지구력 게이트 중 나쁜 쪽**이다. 속도는 되는데 거리를 못 뛰는
사람에게 🟢 를 주면 §7.10 과 정면으로 어긋난다.

**결과**

| 케이스 | 이전 | 지금 |
|---|---|---|
| 하프 1:25 러너 · 풀 6주 · 목표 3:20 | 🔴 | 🟡 |
| 10K 45분 러너 · 하프 5주 · **완주** | 🔴 | 🟡 |
| 하프 1:50 러너 · 하프 14주 · 목표 1:45 | 🔴 | 🟡 |
| 입문자(주 10km) · 하프 5주 · 완주 | 🔴 | 🔴 (유지) |
| 속도만 빠름(주 10km) · 풀 20주 · 목표 3:10 | 🔴 | 🔴 (유지) |

**ENGINE_VERSION 0.2.0 → 0.3.0.** 판정 규칙이 바뀌었으므로 저장된 플랜은 저장 시점 버전으로
계속 렌더링되고, 화면에 `outdated` 로 표시된다 (§9.7). 조용히 바꾸지 않는다.

테스트 105 → 107 (지구력 게이트·완충 경계·역전 방지). §11.3 불변식 전 항목 통과.

### 2026-09-10 에 한 것 — 정식 브랜드 자산 적용

정식 아이콘·로고가 납품돼 임시 마크를 걷어냈다. `npm run brand`(`scripts/build-brand-assets.ts`)가
납품 마스터(`docs/brand/`)에서 `public/brand/` 의 운영 파일을 뽑는다.

**1차 납품본은 쓸 수 없었다** — 디자인 툴의 투명 표시용 **회색 격자가 픽셀로 구워져** 있었다
(알파 255, 색 254↔231 반복). 마스터는 투명 영역이 0% 였다. 빨간 배경에 얹으면 바깥 여백에만
빨강이 비치고 안쪽은 격자가 남는 것으로 확인했다. 재납품받아 해결.

**납품 사이즈를 그대로 쓰지 않는다.** 납품본은 여백이 30% 라 마크가 캔버스의 37% 만 차지하는데,
그 상태로 16px 파비콘을 만들면 마크가 6px 이 되어 셰브론이 통째로 뭉개진다 —
의뢰서 §5 의 **1순위 요건**("16×16px 에서 읽혀야 한다")에 걸린다.
그래서 마크만 잘라 내고 쓰임새별로 여백을 다시 준다. **형태·색은 손대지 않는다.**

| 쓰임 | 여백 | 배경 |
|---|---|---|
| 파비콘 16·32·48 | 8% | 투명 — 작을수록 꽉 채워야 읽힌다 |
| PWA `any` 192·512 | 12% | 투명 — 런처가 자기 판을 깐다 |
| maskable 512 | 18% | **크림** — 안드로이드가 바깥 10% 를 잘라낸다 |
| apple-touch 180 | 12% | **크림** — iOS 는 투명을 검정으로 채운다 |

**자산 위치를 정리했다.** 한때 `public/brand/` 아래에 `pwa/`·`icons/`·`logos/`·`new/` 네 벌이
섞여 있었다. 지금은 **`docs/brand/` = 납품 마스터, `public/brand/` = 서빙되는 것**뿐이다.
마스터가 `public/` 에 있으면 쓰지도 않는 파일이 배포에 딸려 간다.

### 2026-09-10 에 한 것 — 다중 목표 (O16)

목표를 두 개 저장해 두고 화면을 하나씩 봤더니 **실제로 깨지는 곳은 둘**이었다.

**🐛 `/races/[slug]` 의 달력 아이콘이 어느 플랜에서 눌러도 `/log` 로 갔다.**
`/log` 는 `primaryRace()`(가장 가까운 대회)를 그리므로, **서울마라톤 상세에서 눌렀는데
MBN 달력이 떴다.** 두 번째 목표의 달력에는 도달할 방법이 아예 없었다.

- `/log` 가 `?plan=<key>` 를 받는다. 없으면 가장 가까운 대회
- 목표가 **둘 이상일 때만** 선택기를 그린다 — 고를 것이 없는 컨트롤은 소음이다.
  링크라 클라이언트 상태가 필요 없다
- `CalendarLink` 가 그 플랜의 key 를 넘긴다

**`/me` 가 목표를 하나만 보여 줬다** — 2개인데 "MBN 서울마라톤을 준비하고 있어요"라
읽혀서 나머지가 없는 것처럼 보였다. "2개 목표를 준비하고 있어요" / "MBN 외 1개"로 고쳤다.

**`/today` 는 가장 가까운 대회만 보여 준다 — 의도다.** 오늘 뭘 뛰는지는 하나로 답해야 하고,
목록은 대회 탭이 맡는다.

**남은 것:** `/plan/result?p=` 는 여전히 플랜 **하나**를 담는다. 이건 게스트가 링크로
공유·복원하는 성질이라 바꿀 이유가 없다 (§4.2). 저장은 이미 N개다.

### 2026-09-10 에 한 것 — typedRoutes · Prisma 설정 이관

**`typedRoutes` 를 켰다** (Next 16 에서 안정화되어 `experimental` 밖으로 나왔다).
12곳이 걸렸는데 전부 **런타임에 조립하는 링크**였다. 처리 원칙은 하나다 —
**단언(`as Route`)을 URL 을 만드는 지점에만 두고 화면에는 두지 않는다.**
화면마다 캐스팅을 흩으면 그게 곧 검사를 끄는 것이다.

| 만드는 곳 | 반환 |
|---|---|
| `planHref()` | `Route` — base 가 리터럴 유니온이라 안전 |
| `safeCallbackUrl()` | `Route` — 같은 사이트 경로임을 이미 검증했다 |
| `MyRace.href` | `Route` — `/races/${key}` 조립을 어댑터 한 곳으로 모았다 |

컴포넌트 props(`TabBar`·`MyRaceRow`·`KakaoSignInButton`)도 `string` → `Route` 로 좁혔다.

**Prisma 설정을 `prisma.config.ts` 로 옮겼다** (`package.json#prisma` 는 Prisma 7 에서 제거된다).
⚠️ 함정: **설정 파일이 생기면 Prisma 가 `.env` 를 스스로 읽지 않는다.**
`prisma migrate` 가 DATABASE_URL 을 못 찾게 되므로 설정 파일 안에서 루트 `.env` 를 직접 얹는다.

### 2026-09-10 에 한 것 — 아트워크 용량 (O18)

`npm run art` (`scripts/optimize-illustrations.ts`) 신설. **88MB → 1.34MB (98.5% 감소).**

- 1200px WebP(q90). 앱 최대 슬롯 폭이 448px(`scene.tsx` 의 SIZES)이라 DPR 2.7배까지 커버한다
- 같은 1200px 에서 PNG 1279KB / WebP 71KB 로 **18배** 차이. 원본이 전부 불투명이라
  알파가 필요 없고, `next/image` 가 어차피 다시 인코딩하므로 소스 포맷은 화질에 영향이 없다
- 확장자가 `lib/illustrations.ts` 의 `scene()` 헬퍼 **한 줄**에만 있어서 참조 수정은 그게 전부였다

⚠️ **`.git` 은 여전히 97MB 다.** 원본 PNG 가 커밋 `5b8a253` 에 남아 있어서, 지금 줄인 것은
**앞으로의 클론이 아니라 체크아웃 용량**이다. 히스토리까지 줄이려면 재작성 + force-push 가
필요하고 그건 별도 결정이다 (협업자가 생기기 전에만 쌀 수 있다).

### 2026-09-10 에 한 것 — 계측 (GA4, §15)

배포해도 배울 게 없던 상태를 닫았다. §15 이벤트 13개를 붙였다.

```
lib/analytics-events.ts        이벤트 이름·구간화 (순수)
lib/analytics.ts               track() — 측정 ID 없으면 아무것도 안 한다
components/analytics/
  google-analytics.tsx         스크립트 로더 + 프라이버시 설정
  track-event.tsx              서버 컴포넌트가 화면 진입 이벤트를 쏘는 통로
  track-on-submit.tsx          폼 제출 시점 (수행 체크)
  redirect-markers.tsx         서버에서 끝난 일(로그인·저장·이전)을 도착 화면에서
```

**끈 것들** — `allow_google_signals`·`allow_ad_personalization_signals`·`ads_data_redaction`.
구글 신호를 켜 두면 **우리가 수집하지 않기로 한 성별·연령대가 구글 쪽에 쌓인다** (§9.4).
`@next/third-parties` 의 `GoogleAnalytics` 를 안 쓴 이유가 이거다 — config 파라미터를 넘길 자리가 없다.

**보내지 않는 것** — 사용자 식별자(GA4 `user_id`)와 플랜 입력 원본. 게스트/로그인 구분은
속성 하나로 충분하고, 목표 기록·주간 거리는 그 사람의 훈련 정보다. 구간화한 값만 보낸다.

**밟은 함정 둘 (둘 다 조용히 실패한다)**
1. shim 을 `afterInteractive` 로 두면 React 이펙트가 먼저 돌아 `window.gtag` 가 아직 없다.
   **첫 화면 이벤트가 통째로 유실된다.** `beforeInteractive` 여야 한다
2. 그래서 `dataLayer` 에 직접 밀어 넣어 봤는데, GA 는 **`arguments` 객체**를 기대한다.
   배열을 넣으면 이벤트로 처리하지 않고 **조용히 버린다** — 로그에는 보낸 것처럼 찍힌다

**검증 한계:** 실제 GA 속성이 없어 커스텀 이벤트의 **서버 도달**은 확인하지 못했다.
가짜 측정 ID 는 503 을 받고 이후 전송을 멈춘다(수동 호출도 동일). 확인한 것은
① gtag 가 하이드레이션 전에 존재 ② `track()` 이 올바른 이름·속성으로 gtag 를 호출
③ page_view 비콘에 `npa=1`·`ads_data_redaction=true` 가 실려 나감.
**실제 측정 ID 를 넣은 뒤 GA4 실시간 보고서에서 이벤트 도달을 한 번 확인할 것.**

### 2026-09-10 에 한 것 — 로그인 화면 분리 (/login)

'나' 탭이 설정 화면이면서 로그인 화면을 겸하고 있었다. 거기서 두 가지가 어긋나 있었다.

**① UI** — 설정 목록 한가운데에 카카오 버튼과 이메일 폼이 같은 무게로 서서
무엇이 주 경로인지 안 읽혔다. 이메일은 `AUTH_PASSWORD_LOGIN` opt-in 인 **보조 수단**인데
화면에서는 카카오만큼 자리를 차지했다.

**② 🐛 게스트→회원 전환이 깨져 있었다.** 저장 버튼이 `signIn(undefined, { redirectTo })` 로
로그인을 부르면 Auth.js 가 `pages.signIn`(당시 `/me`)으로 보내면서 `callbackUrl` 을 쿼리에 다는데,
**`/me` 의 로그인 버튼이 `redirectTo='/me'` 를 하드코딩하고 있어 그 값을 버렸다.**
로그인은 되는데 저장하려던 플랜이 사라졌다 — §9.5 의 핵심 동선이다.
지난번엔 `/plan/save?p=` 를 직접 호출해 검증해서 통과했고, **실제 UI 경로는 한 번도 안 밟았다.**

**고친 방식**
- `app/(public)/login/page.tsx` 신설. `callbackUrl` 을 받아 **두 제공자 모두에** 전달한다.
  (public) 이라 탭바가 없고 `noindex` 다. 이미 로그인했으면 바로 되돌려 보낸다
- 이메일 폼은 `<details>` 로 접었다. 클라이언트 JS 를 늘리지 않고 하이드레이션 전에도 열린다.
  ⚠️ `<summary>` 에 `display:flex` 를 걸면 **사파리에서 토글이 깨진다** — 레이아웃은 안쪽 span 이 잡는다
- `/me` 는 '로그인' 버튼 한 줄로 줄었다. 저장 버튼은 `/login?callbackUrl=` 로 직접 간다
- `lib/safe-redirect.ts` — **오픈 리다이렉트 방어.** 쿼리에서 온 경로를 그대로 쓰면
  `?callbackUrl=https://evil.example` 로 피싱이 된다. 같은 사이트 경로만 통과시킨다

**로고 자산 주의** — `public/brand/logos/*.png` 는 **디자인 보드에서 잘라낸 파일**이라
'로고 조합 (가로형)' 캡션과 잘린 태그라인이 그림 안에 박혀 있다. 쓰면 안 된다.
`/login` 은 깨끗한 `icons/RUNBACK_symbol_color.png` 를 쓴다. 락업이 필요하면 다시 받아야 한다.

### 2026-09-10 에 한 것 — 수행 체크 (F-12)

**O17 종결.** 진행률·캘린더·'최근 러닝'이 드디어 실제 기록을 본다.
그전까지는 '날짜가 지났으면 완료'로 대신하고 있었는데, 그러면 **일주일을 통째로 쉰 사람에게도
진행률이 올라간다.** 화면이 거짓말을 하고 있었던 셈이다.

- `plan-view.ts` 에 `LogIndex`(날짜 → 기록)를 넣고 `sessionProgress`·`weekItems`·
  `monthGrid`·`monthSummary` 가 전부 이걸 본다. **이 파일은 여전히 저장소를 모른다** —
  화면이 읽어 온 것을 넘겨받을 뿐이라 순수 함수로 남고 테스트가 쉽다
- `MyRace.logs` — 플랜마다 조회하면 N+1 이라 사용자 기록을 한 번에 가져와 planId 로 나눈다
- `lib/log-actions.ts` — 토글. 기록 하나가 바뀌면 **네 화면을 같이 revalidate 한다**.
  한 곳만 갱신하면 다른 탭이 옛 숫자를 들고 있어서 뭘 믿어야 할지 모른다

**'놓침'과 '건너뜀'을 나눴다.** `missed` 는 날짜가 지났는데 기록이 없는 것(우리가 추측한 상태),
`skipped` 는 사용자가 직접 남긴 것이다. 합치면 **"안 뛴 날"과 "안 적은 날"을 구분할 수 없다.**
목록 표식과 캘린더 마커가 형태로 갈린다 (WCAG 1.4.1) — 점선 링 / 빗금 링.

**당일이 아니어도 체크할 수 있다.** 처음엔 '오늘의 훈련' 카드에만 버튼을 뒀는데,
그러면 하루라도 놓치면 영영 기록을 못 남긴다. 달력에서 날짜를 눌러 지난 날을 채운다.

**'최근 러닝'은 계획 거리를 '계획'이라고 적는다.** 실제로 몇 km 를 뛰었는지는 아직 받지 않는다 —
받지 않은 값을 실제인 척 보여 주지 않는다 (§0-3). 상세 입력은 PRD 가 2차로 둔 항목이고,
스키마에 `actualDistanceKm` 이 이미 있으니 필요해지면 `SessionCheck` 만 늘리면 된다.

**테스트 15 → 30개.** 브라우저로는 '오늘'을 옮길 수 없어 `missed` 경계를 눌러 볼 수 없다.
진행률이 거짓말을 하느냐가 걸린 자리라 순수 함수로 고정해 뒀다 (`plan-view-logs.test.ts`).

### 2026-09-10 에 한 것 — 탈퇴·내보내기 (F-18/F-19)

**유일한 법적 릴리즈 블로커가 닫혔다.** 저장소 함수는 Phase 1 에 있었고 이번에 화면과 Cron 을 붙였다.

- `/me/withdraw` — **되묻는 단계를 늘리지 않았다.** §9.6 이 "탈퇴는 가입만큼 쉬워야 한다"로
  못 박아 뒀다. 대신 무엇이 사라지는지(플랜 N개·기록·계정)는 정확히 보여 준다.
  붙잡는 문구도, 회색으로 죽인 버튼도 없다
- `/api/me/export` — F-19 열람권. `providerUserIdHash`·`passwordHash` 는 빼되
  **그런 값이 있다는 사실은 알려 준다** (숨기는 게 아니라는 뜻). `Cache-Control: no-store, private`
- `/api/cron/purge` + `vercel.json` — ⚠️ **이게 돌지 않으면 방침이 거짓말이 된다.**
  `CRON_SECRET` 없으면 503 으로 닫는다 — 설정 누락이 곧 공개가 되면 안 된다
- `lib/kakao-unlink.ts` — best-effort. **삭제를 여기에 걸지 않는다.** 카카오가 죽어도 탈퇴는 진행된다.
  액세스 토큰은 세션 쿠키에만 두고 DB 에 넣지 않는다 (유출 대상을 늘리지 않는다)

**브라우저로 전 과정을 실제로 클릭해 검증했다** — 로그인 → 탈퇴 화면 → 탈퇴 →
완료 화면 → 재로그인 차단 → 15일 전 purge 0건 → 16일 뒤 purge 1건 →
플랜·기록 Cascade 삭제(0/0/0) → 같은 이메일 재가입은 **새 계정**.

**같이 잡은 배포 위험**
- Prisma 생성 클라이언트의 동적 파일 접근 때문에 Turbopack 이 **프로젝트 전체를 서버 번들에
  끌어넣고 있었다.** public/illustrations 의 89MB 가 서버리스 함수에 딸려 가면 Vercel 250MB
  제한에 걸린다. `outputFileTracingExcludes` 로 잘라 냈다 — 추적 이미지 0개, 합계 37.6MB

### 2026-09-10 에 한 것 — PWA + 루트 진입

"처음 들어오면 로고가 보였다가 앱으로" 를 **루트를 건드리지 않고** 만들었다.
루트를 스플래시·로그인으로 바꾸면 §13 SEO 전략과 §9.2 저장 게이트가 동시에 깨진다.

- `app/manifest.ts` — `start_url: '/today'`. 스플래시는 우리가 그리는 게 아니라
  **OS 가 아이콘 + background_color 로 만든다.** background_color 를 `--color-canvas` 와
  같은 크림으로 맞춰야 실행 순간 배경이 튀지 않는다
- `src/proxy.ts` — 세션 쿠키가 있을 때만 `/` → `/today`. matcher 는 `/` 하나뿐이다.
  쿠키의 **존재만** 본다 (엣지에 배포될 수 있어 DB·비밀키에 기대면 안 되고, 틀려도
  `/today` 가 게스트에게도 정상 동작해서 손해가 없다)
- `public/brand/` — **임시 아이콘.** 정식 로고는 `docs/brand-logo-brief.md` 로 의뢰 중이고,
  납품되면 같은 이름으로 덮어쓰면 된다 (코드 수정 불필요). 의뢰서가 정한 파일명을 그대로 썼다

### 2026-09-10 에 한 것 — 이메일 로그인 + 저장 (F-17)

**이메일 + 비밀번호 로그인** — PRD §9.3 이 원래 만들지 않기로 했던 것을 테스트 경로로 열되,
**남을 수 있다는 전제**로 제대로 만들었다 (PRD v1.4 에 예외로 기록).

- `packages/db/src/password.ts` — scrypt(N=2^15). Node 내장이라 네이티브 의존성이 없다.
  파라미터를 해시 문자열에 같이 적는다 — **나중에 비용을 올려도 옛 해시가 검증된다**
- `packages/db/src/password-auth.ts` — 계정 열거 방어가 핵심이다. 실패 사유를 구분하지 않고,
  없는 계정에도 더미 해시로 **같은 시간을 쓴다**(측정 결과 비율 0.95). 10회 실패 시 15분 잠금
- **이메일을 평문으로 저장하지 않는다.** `providerUserIdHash` 에 해시로 넣어 소셜과 같은 신원
  모델을 쓴다 — 활성 유니크 인덱스·soft delete·15일 purge 가 전부 그대로 적용된다.
  덕분에 §9.4 의 '이메일 미수집'을 깨지 않는다. 대가는 재설정 메일을 못 보낸다는 것
- 켜는 건 `AUTH_PASSWORD_LOGIN=true` **명시적 opt-in**. 환경으로 자동 판단하지 않는다 —
  스테이징·프리뷰처럼 애매한 환경에서 예측이 안 된다
- 계정 생성은 CLI 뿐 (`user:create`). 열린 가입 엔드포인트는 스팸가입 대응이 먼저다

**저장 F-17 + 게스트→회원 전환 §9.5**
- `savePlan()` 이 **멱등**하다 — 같은 사용자·같은 입력이면 새로 만들지 않는다.
  이게 있어야 전환 경로가 새로고침·뒤로가기에 안전하다
- 전환은 localStorage 가 아니라 **콜백 경로**로 간다 (`/plan/save?p=...`).
  사파리 저장소 제한·시크릿 창·기기 전환에 영향받지 않고 클라이언트 JS 도 필요 없다 (PRD §9.5 갱신)
- `npm run legal` 신설 — 방침 플레이스홀더 / **이메일 수집과 방침의 일치** / 국외 이전 조항을 검사.
  "코드가 방침보다 앞서 나가면 위반"이라는 규칙에 드디어 장치가 붙었다

**같이 고친 것**
- **`RUNBACK_DEV_USER_ID` 우회로 제거.** 개발 중에 항상 로그인 상태로 보여서
  **게스트 경로를 테스트할 수 없었다** — 게스트 흐름은 이 제품의 주 동선이다 (§9.2).
  비밀번호 로그인이 생겨 필요도 없어졌다
- **`MyRace.race` 를 옵셔널로.** 위저드가 날짜 직접 입력을 지원하는데(F-01) 그렇게 만든 플랜은
  `raceSlug` 가 없어서 **저장은 되지만 내 대회에 영영 안 떴다.** 화면은 이제 `name`/`date`/`key` 를
  쓴다. 대회 데이터에서 지난 대회가 빠져도 저장한 플랜이 사라지지 않는다
- 시드가 'dev-user' 대신 **로그인 가능한 계정**에 붙는다. 로그인 못 하는 계정에 플랜이 달리면
  실제 화면으로 확인이 안 된다

### 2026-09-10 에 한 것 — 인증 (F-16)

Auth.js v5 + 카카오. `next-auth@5.0.0-beta.32` 가 `next ^16` 을 peer 로 선언해 PRD §9.8 선택이 그대로 성립했다.

- `src/auth.ts` — 카카오 provider. **기본 `profile()` 을 덮어썼다.** 기본값이 닉네임·이메일·
  프로필 이미지까지 매핑하는데 §9.4 가 셋 다 미수집으로 정해 뒀다. `scope` 도 비운다 —
  **"회원번호만 수집"이 §9.3 의 심사 비대상 근거**라 여기가 흔들리면 사업자등록 얘기가 돌아온다
- `app/api/auth/[...nextauth]/route.ts` — 이 리포의 **유일한** route handler. Node 런타임 고정(Prisma)
- `lib/session.ts` — Phase 1 에서 좁혀 둔 덕에 인증을 붙이며 고친 파일은 이것 하나
- `components/auth/auth-buttons.tsx` — 클라이언트 컴포넌트가 아니다. form action 에 Server Action 을 건다
- 카카오 브랜드 색을 토큰(`--color-kakao` / `--color-on-kakao`)으로 넣고 대비 계약에 등록 (14.20:1)

**proxy.ts(구 middleware)를 만들지 않았다.** Next 16 에서 `middleware.ts` 는 `proxy.ts` 로 이름이
바뀌었는데, 애초에 라우트를 막지 않으므로 파일 자체가 필요 없다. 공개 경로와 플랜 결과는
크롤러가 도달해야 하고(§13), 로그인은 저장 시점에만 요구한다(§9.2). 보호 대상은 페이지가 아니라 동작이다.

**확인한 것**
- OAuth 요청이 `scope=` 비어 있고 PKCE(S256)로 나간다 — 회원번호만 받는다는 게 실제로 그렇다
- 공개 경로 6개 전부 200, 사전 렌더 37개 라우트 유지 (인증이 SEO 를 건드리지 않았다)
- DB 경로 7항목: 최초 로그인 → 재로그인 동일 계정 → **원본 카카오 번호 미저장** → 탈퇴 즉시 차단
  → 재가입은 새 계정 → 15일 전 purge 0건 → 16일 뒤 purge 1건
- 플랜이 0건인 사용자로 앱 셸 4개 화면 — 전부 빈 상태로 정상 렌더 (로그인 직후 반드시 겪는 상태다)

**같이 고친 버그**
- 카카오 버튼을 `className` 으로 덮었더니 `bg-brand` 와 `bg-kakao` 가 특이도가 같아
  생성된 CSS 순서에 따라 색이 갈렸다. `Button` 의 `kakao` variant 로 옮겼다

### 2026-09-09 에 한 것
- 대회 탭을 **전체 브라우징 → 내 대회**로 재정의. 브라우징은 탭바 없는 `/race` 로 분리 (**O15 해소**)
- `/races/[slug]` 훈련 일정 상세 신설 (세션 카운터, 오늘의 훈련, 이번 주, 전체 주차 아코디언)
- `/log` 월 캘린더 신설 (4마커, 월 이동, 날짜 탭하면 세션 펼침)
- `/me` 프로필·통계·설정 메뉴로 재작성
- 진행률을 날짜 경과 비율 → **세션 완료 수** 기준으로 교체 (**O17 표시 기준 해소**)
- 앱 셸 헤더를 레이아웃에서 화면으로 분리 (`AppScreen`)
- **플랜 시작 전 상태** 신설 — 대회 날짜에서 주 단위로 역산하므로 첫 주가 다음 주 월요일부터일 수 있는데,
  그걸 "오늘은 휴식일"이라고 하면 플랜이 이미 돌아가는 것처럼 읽힌다

### 2026-09-09 에 한 것 — 디자인 리뉴얼
크림+보라를 유지하고 위계·리듬·활자를 다시 짰다. 이후 작성자 목업(`docs/mockups/`)이
들어와서 화면 구성을 목업 기준으로 다시 맞췄다.

**디자인 시스템**
- 활자 스케일 11단계 도입, 임의 크기 19종 → 0 (30개 파일 일괄 치환)
- 카드 톤 3단계(plain/raised/sunken). `raised` 는 화면당 하나
- `<Screen>`/`<Section>`/`<Badge>`/`<ZoneBadge>`/`<MenuRow>`/`<MiniStat>`/`<PlanStateCard>` 신설
- `word-break: keep-all` — 한글 어절 중간 줄바꿈 제거
- 대비 계약 확장 중 기존 위반 발견·수정: `ink-muted` 가 `surface-sunken` 위에서 4.26:1 → `#6e685e` (4.87:1)
- `check-contrast` 에 미등록 토큰 검사 추가

**목업에 맞춘 것**
- NEXT RACE 카드를 **흰 카드 + 검정 D-day** 로. 한때 보라로 꽉 채웠는데 목업과 어긋났다
  (보라 채움 팔레트 `brand-deep`·`brand-shade`·`on-brand-muted` 는 같이 걷어냈다)
- 홈의 TODAY'S TRAINING 은 카드가 아니라 캔버스에 flush, 그 아래 큰 주 버튼
- `/races` 상단에 NEXT RACE 히어로 추가, 내 대회 행은 이름·날짜·목표로 단순화
- `/races/[slug]` 히어로를 카드에서 빼고, '오늘의 훈련' 카드 + 주차 점 인디케이터
- 캘린더: **월요일 시작**, ~~영문 월 이름~~ → **한글 (`2026년 9월`)**, 범례 상단, 마커를 점 → 아웃라인 아이콘
  (목업은 `September 2026` 과 `MON/TUE` 였는데 요일은 일찍이 한글로 바뀌어 있었다. 2026-09-11 에 월 이름도 한글로 맞췄다 — §8 아래 참조)
- `/me` 헤더를 로고 → 제목형(`TitleHeader`), 아이콘 메뉴 카드
- 탭바 활성 표시에서 알약 배경 제거 — 색 + 선 굵기 + 라벨 굵기로 간다
- 일러스트 파이프라인을 `next/image` 로 (자동 리사이즈·WebP). 세션 타입별 씬 슬롯 8개 추가

**같이 고친 버그**
- `/today` 인사말: `getUTCHours()+9` 가 24를 넘어 KST 새벽에 '좋은 저녁이에요'가 떴다

### 목업과 일부러 어긋난 자리

| 항목 | 목업 | 구현 | 이유 |
|---|---|---|---|
| 달력 월 이름 | `September 2026` | **`2026년 9월`** | 요일이 이미 한글(월·화·수)이라 한 달력 안에서 표기가 섞였다. `/race` 는 **한국어 검색으로 들어오는 공개 페이지**인데 영문 머리글 바로 아래에 '11월 대회 21개'가 오는 모양이 됐다 |
| 달력 요일 | `MON TUE …` | **`월 화 …`** | 위와 같은 이유. 구현 초기부터 한글이었다 |

### 의도적으로 안 만든 것 (지워버리지 말 것)
| 항목 | 이유 |
|---|---|
| 설정 메뉴의 '준비 중' 항목 | 없는 기능을 링크로 두지 않는다 |
| **성별** 입력 | 목업엔 있으나 PRD §9.4 가 미수집으로 정해 뒀다. 수집 최소화 위반 (O13 미결) |
| **디바이스 연동** | 웹에서 애플·삼성 헬스 연동은 물리적으로 불가. §3.3 Non-goal (O14) |
| 목업의 **'오늘 달리기'** 버튼 | 러닝 트래킹이 없다. 버튼의 위치·무게는 그대로 두고 문구만 '오늘 훈련 자세히 보기'로 바꿔 실제 갈 수 있는 곳으로 보낸다 |
| 목업의 **주간 목록 화살표** | 세션 상세 화면이 없다. 갈 곳이 생기면 `SessionRow` 에 `href` 만 넘기면 화살표가 돌아온다 |
| 목업 '나' 탭의 **톱니 아이콘** | 이 화면 자체가 설정이라 갈 곳이 없다 |
| 목업 '최근 러닝'의 **실제 거리·시간·기분** | F-12 는 완료 체크까지다. 실제로 뛴 거리를 받지 않으므로 '계획 7km' 로 적는다 — 받지 않은 값을 실제인 척 쓰지 않는다 (§0-3). 상세 입력은 PRD 2차 |

---

## 9. 지금 막혀 있는 것

### 사용자 결정 대기
| # | 내용 | 영향 |
|---|---|---|
| ~~O1'~~ | ~~도메인 확정~~ | ✅ **해소.** `runback.kr` · `privacy@runback.kr`. 남은 건 `effectiveDate` 뿐이고 이건 **인증을 켜는 날** 정해진다 |
| **신규** | **카카오 앱 키** | 인증 코드는 다 붙었고 `AUTH_KAKAO_ID` / `AUTH_KAKAO_SECRET` 만 비어 있다. developers.kakao.com 에서 앱을 만들고 Redirect URI 에 `/api/auth/callback/kakao` 를 등록해야 로그인이 실제로 돈다 |
| **신규** | **`privacy@runback.kr` 실제 수신** | 방침에 적힌 연락처가 반송되면 연락처를 안 적은 것과 다르지 않다. 도메인 구매 후 Cloudflare Email Routing 등으로 포워딩을 걸어야 한다 |
| O13 | 성별 수집 여부 | 받으려면 엔진이 성별을 실제로 써야 한다 (§7.4 테이퍼 차이) |
| O14 | 디바이스 연동 | Strava(본인 데이터 읽기)만 가능성 있음 |
| O5 | 캘린더 내보내기 MVP 포함 여부 | |
| O10 | 게스트 플랜 URL 유효기간 | `engineVersion` 과 연결 |
| ~~O18~~ | ~~아트워크 원본 용량~~ | ✅ **해소 (2026-09-10).** 1200px WebP 재출력으로 88MB → 1.34MB (`npm run art`). ⚠️ 원본 PNG 가 커밋 `5b8a253` 에 남아 있어 **`.git` 은 여전히 101MB** 다 — 줄어든 것은 체크아웃 용량이지 클론 용량이 아니다. **이미 GitHub 에 푸시됐으므로** 히스토리까지 줄이려면 재작성 + force-push 가 필요하고, **협업자가 생기기 전에만 쌀 수 있다** (§10-B) |
| O19 | 씬의 성별 선택 | 두 벌 다 리포에 있고 `lib/illustrations.ts` 의 `SCENE_SET` 상수가 고른다(현재 `'female'`). 러너에 맞춰 자동으로 고르려면 O13(성별 수집)이 먼저다 |

`lib/legal.ts` 의 `hasUnresolvedLegalInfo()` 가 `TODO_` 잔존 여부를 알려준다. **`true` 인 상태로 인증을 켜면 실제 위반이다.**

### 구현 부채
| # | 내용 |
|---|---|
| O16 | 여러 목표 동시 관리 — UI(`myRaces()`)는 배열로 열어 뒀지만 **엔진·URL 구조는 아직 플랜 1개 기준** |
| ~~O17~~ | ✅ **종결.** F-12 가 붙어 진행률·캘린더가 실제 기록을 본다 |
| — | ~~`lib/demo-plan.ts`~~ ✅ 삭제됨. 이제 `lib/my-races.ts` 가 DB 를 읽는다 |
| — | **입문자 notice 가 지키지 못할 약속을 한다.** `fitness.ts` 가 '입문자 프리셋으로 추정했습니다. **첫 2주 기록으로 다시 맞춥니다**' 를 띄우는데 **다시 맞추는 기능이 없다.** F-12 는 완료 체크까지라 실제 거리·시간을 안 받고, `actualDistanceKm` 은 Prisma 생성 타입에만 있고 앱 코드에서 쓰는 곳이 없다. 신뢰도가 가장 낮은 사용자에게 '곧 정확해집니다'라고 해 놓고 영영 안 맞추는 셈이라 §0-3 에 걸린다. **고치는 길은 둘** — ⓐ 문구에서 약속을 빼거나(한 줄), ⓑ §10-C 1번(수행 기록 상세 입력)을 만들어 실제로 재추정한다 |
| — | 비밀번호 재설정이 없다. 이메일을 평문으로 저장하지 않기 때문이다(의도). 필요해지면 평문 컬럼 + 메일 발송 + **방침의 처리 항목**이 한 세트로 따라온다 |
| — | 공개 가입 화면이 없다. 계정은 CLI 로만 만든다 — 열린 가입 엔드포인트는 스팸가입 대응(레이트 리밋·캡차)이 먼저다 |
| — | `/privacy` 에 `TODO_YYYY-MM-DD` 가 그대로 찍힌다. 시행일은 인증을 켜는 날 정해진다 — `npm run legal` 이 이걸 잡는다 |
| — | ~~O16 다중 목표~~ ✅ **앱 셸은 N개를 다룬다.** 남은 것은 `/plan/result?p=` 가 플랜 **하나**를 담는다는 점인데, 이건 게스트 공유 링크의 성질이라 바꿀 이유가 없다 (§4.2) |
| — | ~~Prisma 설정~~ ✅ **`prisma.config.ts` 로 이관.** ⚠️ 설정 파일이 생기면 Prisma 가 `.env` 를 스스로 읽지 않아서 파일 안에서 루트 `.env` 를 직접 얹는다 |
| — | ~~`typedRoutes` 꺼짐~~ ✅ **켰다.** URL 을 만드는 헬퍼(`planHref`·`safeCallbackUrl`·`MyRace.href`)가 `Route` 를 내고, 단언은 거기서만 한다 |

---

## 10. 다음 작업 후보

**기능은 MVP 범위를 다 채웠다.** 남은 것은 대부분 **배포와 그에 딸린 외부 작업**이고,
코드로 할 일은 그다음이다. (여기 있던 1~6번 항목은 2026-09-10~11 에 전부 종결됐다.)

### A. 릴리즈 — 지금 가장 앞

순서가 있다. 앞의 것이 안 되면 뒤의 것을 해도 의미가 없다.

| # | 할 일 | 누가 | 막는 것 |
|---|---|---|---|
| A1 | **도메인 `runback.kr` 구매 + DNS** | 사용자 | 아래 전부 |
| A2 | **`privacy@runback.kr` 수신 설정** (Cloudflare Email Routing 등) | 사용자 | 방침에 적힌 연락처가 반송되면 **연락처를 안 적은 것과 같다** (법 제30조) |
| A3 | **카카오 앱 생성** — `AUTH_KAKAO_ID`/`SECRET`, Redirect URI `/api/auth/callback/kakao` | 사용자 | 로그인. 코드는 다 붙어 있다 |
| A4 | **Neon 프로젝트 + `DATABASE_URL`**, `npm run db:migrate` 배포 | 사용자 | 계정·저장 |
| A5 | **Vercel 배포** + 환경변수(`AUTH_SECRET`·`AUTH_ID_PEPPER`·`CRON_SECRET`) | 사용자 | |
| A6 | **`LEGAL.effectiveDate` 를 실제 시행일로** → `npm run legal` 0건 | 코드 한 줄 | **이게 남아 있으면 인증을 켜는 순간 실제 위반이다** |
| A7 | 파기 Cron 이 실제로 한 번 돌았는지 확인 | 사용자 | 방침이 약속한 15일 삭제 |
| A8 | **GA4 속성 생성 → `NEXT_PUBLIC_GA_MEASUREMENT_ID`**, 실시간 보고서에서 이벤트 도달 확인 | 사용자 | §15 계측. 가짜 ID 로는 **서버 도달을 확인할 수 없다** |

> A6 은 **A1~A5 가 끝난 날**에 정해진다. 시행일은 "실제로 개인정보 처리를 시작하는 날"이고
> 그게 곧 카카오 로그인을 켜는 날이다.

### B. 배포 전에만 쌀 수 있는 것

| # | 할 일 | 왜 지금인가 |
|---|---|---|
| **O18** | `.git` **101MB** — 원본 PNG 88MB 가 커밋 `5b8a253` 에 남아 있다 | 이미 GitHub 에 푸시됐다. 지우려면 히스토리 재작성 + force-push 가 필요하고, **협업자가 생기면 그때는 못 한다.** 지금은 혼자라 아직 창이 열려 있다 |

### C. 코드로 남은 것 (우선순위 순)

1. **수행 기록 상세 입력** — F-12 는 완료 체크까지다. 실제로 뛴 거리·시간을 받지 않아
   '최근 러닝'이 '계획 7km'라고 적는다. 스키마에 `actualDistanceKm` 이 이미 있어서
   `SessionCheck` 만 늘리면 된다. PRD 는 2차로 뒀지만 **도그푸딩하면 제일 먼저 아쉬운 자리**다.
   입문자 notice 의 '첫 2주 기록으로 다시 맞춥니다'도 이게 있어야 참이 된다 (§9 구현 부채).
1-1. **입문자 안내의 나머지 두 갈래** — 세션 수행 가이드(§7.8-1)는 붙었고, 남은 것은 둘이다.
   ⓐ **입문자 목표 랜딩** — `/goal/*` 13개가 전부 기록 목표(sub-X)라 **P1 용이 하나도 없다.**
   `첫 5K 완주`·`첫 10K 완주` 를 넣으면 P1 검색어(`하프 처음 준비`)와 바로 이어지는데,
   §13.2 규칙 3 때문에 **문장을 목표마다 손으로 써야 한다.**
   ⓑ **독립 가이드 문서**(§13.4 콘텐츠 클러스터) — 가장 크고 §13.2 위반 위험도 제일 크다.
   대회 준비·테이퍼의 뜻·전날 체크리스트가 여기 들어간다.
2. **F-13 LLM 수행 코치** (§7.9, v1.7 에서 범위 변경) — 주간 리뷰 + **재판정 제안**.
   원안의 '주차 코멘트'는 보류다 (O21).
   ⚠️ **이 기능의 실제 작업은 프롬프트가 아니라 엔진 모듈**이다. 이탈 패턴을 분류하는 순수 함수가
   먼저 있어야 하고, LLM 은 그 진단을 문장으로 바꾸기만 한다. 로그를 통째로 넣고 "코칭해줘"라고 하면
   모델이 볼륨·강도 숫자를 지어내면서 **ACWR 클램프와 존 상한을 전부 우회한다** — 부상 경로다.
   `note` 에 통증이 적히면 그 세션은 LLM 에 넘기지 않고 §7.10 고정 문구로 보낸다.
3. **O10 게스트 URL 유효기간** — `engineVersion` 이 올라간 링크를 어떻게 다룰지.
   지금은 무기한이고 저장된 플랜만 `outdated` 표시가 있다.
4. **F-14 PDF·이미지 내보내기** / **F-15 제휴 슬롯** — 둘 다 P1. 제휴는 트래픽을 보고 판단한다.

### D. 결정이 먼저인 것

| # | 내용 | 여는 조건 |
|---|---|---|
| O13 | 성별 수집 | 엔진이 성별을 **실제로 써야** 한다 (§7.4 테이퍼 차이). 받아만 두면 수집 최소화 위반 |
| O14 | 디바이스 연동 | Strava(본인 데이터 읽기)만 가능성 있음. 애플·삼성 헬스는 웹에서 물리적으로 불가 |
| O20 | 대회×종목·대회×목표 SEO 페이지 (~250개) | **페이지마다 고유한 문장을 쓸 사람과 시간.** 그 전에는 §13.2 위반이다 |
| O6 | 러닝화 제휴 카본화 컷오프 | 2차 |

---

## 11. 함정 모음

실제로 밟았던 것들.

| 증상 | 원인 / 해결 |
|---|---|
| `.ts` 파일에서 `console.log` 가 빨간 줄 | 에디터가 워크스페이스 TypeScript 를 안 쓰고 `@types/node` 를 못 찾은 것. `.vscode/settings.json` 의 `typescript.tsdk` + `packages/engine/tsconfig.json` 의 `types: ["node"]` 명시로 해결. **`tsconfig.purity.json` 의 `types: []` 는 의도된 것**이라 건드리지 말 것 — 엔진의 Node 무의존을 강제하는 장치다 |
| `node --test test/` 가 MODULE_NOT_FOUND | 디렉터리가 아니라 글로브를 준다 — `node --test test/*.test.ts` |
| 워크스페이스 `.ts` 확장자 import 가 타입체크 실패 (TS5097) | `apps/web/tsconfig.json` 의 `allowImportingTsExtensions: true` |
| `/today` 가 정적으로 굳어 "오늘"이 멈춤 | 날짜에 의존하는 앱 셸 페이지는 `export const dynamic = 'force-dynamic'` |
| 존별 주간 거리 상한이 8%로 새어 나감 | 반복 횟수에 `Math.round` 대신 `Math.floor`. 상한은 총거리가 아니라 `zoneKm`(워밍업 제외)으로 계산 |
| 테이퍼 주차가 피크보다 무거워짐 | PRD 의 레이스주 0.55 를 그대로 쓰면 풀코스에서 역전된다. `periodization.ts` 의 taperCurve 는 **의도적으로 PRD 와 다르다** |
| 예산이 빠듯하면 테이퍼 강도가 떨어짐 | 세션 축소 순서: 롱런 → 보조 퀄리티(M) → 워밍업 → **주 퀄리티는 맨 마지막** |
| `notFound()` 인데 HTTP 200 | `loading.tsx` 가 있으면 셸이 먼저 flush 되어 상태 코드가 200으로 나간다. not-found UI 자체는 정상 표시된다 |
| `.claude/skills/` 가 변경 목록에 계속 뜸 | 범용 디자인 스킬이라 프로젝트가 아니라 `~/.claude/skills/` 에 둔다. `.gitignore` 에 `.claude/` 를 넣어 뒀다 |
| `docs/mockups/` 와 `public/illustrations/` 에 같은 그림이 두 벌 | 아트워크의 자리는 `public/` 이다. 목업 폴더에는 **화면 목업만** 둔다 — 둘 다 커밋하면 같은 46MB 를 두 번 담는다. 성별 변형은 중복이 아니라 **다른 그림**이니 지우지 말 것 |
| 크림 배경에선 통과하던 색이 다른 표면에서 미달 | `ink-muted` 가 `canvas` 위 4.53:1 인데 `surface-sunken` 위에선 4.26:1 이었다. 대비 계약의 `on` 목록에 **텍스트가 실제로 얹히는 표면을 전부** 적을 것 |
| 한글이 어절 중간에서 끊김 ('기간' / '에') | 브라우저 기본 줄바꿈. body 에 `word-break: keep-all` 을 건다 |
| KST 새벽에 '좋은 저녁이에요' | `new Date().getUTCHours() + 9` 가 24를 넘는다. `% 24` 를 잊지 말 것 |
| 투명 PNG 를 만들었는데 검은 바닥이 생김 | sharp 의 `flatten()` 은 **알파 0 인 배경을 넘겨도 알파 채널을 없앤다.** 투명을 유지할 땐 아예 부르지 말 것 |
| 다크 탭에서 파비콘이 안 보임 | 브랜드 보라가 어두워서다(다크 탭 1.7:1). 색을 하나로 맞추려 하지 말고 `media="(prefers-color-scheme: dark)"` 로 파일을 나눈다. **다크 항목을 먼저** 적어야 한다 |
| 캘린더가 .ics 를 통째로 거부하거나 일정이 중복됨 | 포맷 문제다. **75 옥텟**(바이트) 줄 접기 · CRLF · UID 안정성 셋을 먼저 본다 (`calendar-ics.test.ts` 가 전부 검사한다) |
| iOS 에서 .ics 가 캘린더로 안 열림 | `<a download>` 을 붙였을 때다. 서버의 Content-Disposition 에 맡긴다 |
| 실력이 충분한데 🔴 이 나옴 | 달력·볼륨 규칙이 실력을 덮어쓰고 있는지 본다. 판정은 **gap/capacity 와 지구력 게이트**에서만 나와야 하고, 기간 미달과 볼륨 부족은 **한 단계 하향**까지다 |
| 엔진 계수를 고쳤는데 저장된 플랜이 그대로 | 정상이다. `engineVersion` 으로 고정 렌더링한다 (§9.7). 계수를 바꿨으면 `ENGINE_VERSION` 을 올릴 것 — 안 올리면 저장된 플랜이 **조용히** 달라진다 |
| 다른 대회의 달력이 뜸 | `/log` 는 `?plan=<key>` 로 대상을 받는다. 링크를 만들 때 key 를 빠뜨리면 가장 가까운 대회가 열린다 |
| dev 서버가 CSS 파싱 오류로 500 | Turbopack 캐시가 깨진 것이다 (소스는 멀쩡). `.next-agent` 를 지우고 재시작한다 |
| `prisma migrate` 가 DATABASE_URL 을 못 찾음 | `prisma.config.ts` 가 있으면 Prisma 가 `.env` 를 자동으로 읽지 않는다("skipping environment variable loading"). 설정 파일에서 `process.loadEnvFile()` 로 얹는다 |
| `'use client'` 파일에 import 를 넣었더니 컴파일 실패 | 지시어가 **파일 첫 줄**이어야 한다. import 를 그 위에 두면 안 된다 |
| GA 이벤트가 하나도 안 나감 | shim 이 `afterInteractive` 면 이펙트가 먼저 돌아 `window.gtag` 가 없다. `beforeInteractive` 로 둔다 |
| GA 이벤트가 보낸 것처럼 보이는데 집계에 없음 | `dataLayer.push` 에 **배열**을 넣었을 때다. GA 는 `arguments` 객체를 기대하고 배열은 조용히 버린다 |
| dev 에서 방금 고친 게 반영 안 된 것처럼 보임 | 편집 직후 바로 이동하면 stale 청크가 온다. 계측처럼 **조용히 실패하는 것**을 디버깅할 땐 서버를 재시작하고 확인할 것 |
| 로그인은 됐는데 저장하려던 게 사라짐 | `callbackUrl` 을 받아 놓고 안 쓴 것이다. 로그인 진입점이 여러 개면(저장 버튼·나 탭) **전부** 이 값을 넘겨야 한다. 검증할 땐 리다이렉트 목적지를 직접 호출하지 말고 **버튼부터 눌러 볼 것** |
| `<details>` 가 사파리에서 안 열림 | `<summary>` 에 `display:flex` 를 걸면 그렇다. 레이아웃은 안쪽 요소가 잡는다 |
| 로고에 캡션 글자나 회색 격자가 같이 나옴 | 디자인 툴의 **미리보기 캡처**를 받은 것이다. 알파를 재 보면 안다 — 투명이 0% 거나 회색 불투명이 절반이면 캡처다. 빨간 배경에 얹어 보면 바로 보인다. 해결은 재납품뿐이고, 회색을 키아웃하면 가장자리에 프린지가 남아 16px 에서 지저분해진다 |
| 배포 번들에 public/ 전체가 딸려 감 | Prisma 클라이언트의 동적 fs 접근을 Turbopack 이 보고 프로젝트 전체를 추적한다. `next.config.ts` 의 `outputFileTracingExcludes` 로 자른다. 빌드 경고는 남지만 실제 추적에서는 빠진다 (`.nft.json` 으로 확인) |
| 서버 액션을 curl 로 테스트하려다 500 | RSC 액션 프로토콜이라 흉내내기 어렵다. 브라우저로 실제 클릭해서 확인하는 편이 빠르다 |
| maskable 아이콘이 일반 아이콘과 바이트가 같음 | 안전 영역 변환이 안 걸린 것이다. `logo-mark-maskable.svg` 를 **별도 파일**로 두는 이유 — 한때 문자열 치환으로 만들었다가 SVG 를 고치면서 조용히 원본과 같아졌다 |
| `'use server'` 파일이 통째로 컴파일 실패 | 그 파일은 **async 함수만** export 할 수 있다. 동기 헬퍼가 하나라도 섞이면 모듈 전체가 죽는다 (`planTitle` 을 `plan-title.ts` 로 분리한 이유) |
| 저장한 플랜이 내 대회에 안 뜸 | `raceSlug` 가 없거나 대회 데이터에서 빠진 경우다. `MyRace.race` 는 옵셔널이고 화면은 `name`/`date`/`key` 를 쓴다 — `race.nameKo` 를 직접 쓰지 말 것 |
| `pnpm --filter X script -- 인자` 에서 `--` 가 인자로 들어옴 | pnpm 이 그대로 넘긴다. CLI 에서 걷어낸다 (`cli-user.ts`) |
| Next 16 에서 `middleware.ts` 가 안 먹음 | **`proxy.ts` 로 이름이 바뀌었다.** 이 프로젝트는 라우트를 막지 않으므로 아예 만들지 않는다 — 만들고 싶어지면 §9.2 를 먼저 읽을 것 |
| 카카오 버튼 색이 보라로 나옴 | `variant="primary"` 를 둔 채 `className` 으로 `bg-kakao` 를 덮었다. Tailwind 유틸리티는 특이도가 같아서 **클래스 문자열 순서가 아니라 생성된 CSS 순서**가 이긴다. 색을 바꾸려면 `Button` 의 variant 를 늘린다 |
| 로그인했더니 세션 타입이 안 맞음 | Auth.js 의 `Session['user']` 는 `AdapterUser` 와 교차한다. 통째로 갈아끼우지 말고 `DefaultSession['user']` 와 교차시키고 콜백에서 **스프레드**할 것 |
| Next 가 `DATABASE_URL` 을 못 읽음 | Next 는 **앱 디렉터리(`apps/web`)의 `.env` 만** 읽는다. 이 리포는 루트 `.env` 한 곳에 두므로 `next.config.ts` 가 `process.loadEnvFile()` 로 루트 파일을 얹는다 |
| 탈퇴한 사용자가 재가입을 못 함 | `providerUserIdHash` 에 **전역** 유니크를 걸면 soft delete 된 행이 남아 15일간 가입이 막힌다. 부분 유니크 인덱스(`WHERE "deletedAt" IS NULL`)여야 하고, **Prisma 스키마 문법에 없어서** 마이그레이션 SQL 로 직접 넣었다 (`20260910001600_active_provider_id_unique`) |
| 저장된 플랜의 날짜가 하루 밀림 | `SessionLog.date` 를 `DateTime` 으로 두면 UTC 변환이 끼어든다. 이 리포의 날짜는 전부 **KST ISO 문자열**이라 `String @db.VarChar(10)` 로 저장한다 |
| 저장된 플랜을 오늘 날짜로 다시 생성 | `generatePlan` 에 넘기는 `today` 는 **저장 시점의 `input.today`** 다. 오늘 날짜를 넣으면 플랜이 매일 달라진다 — 결정론(§4.2)이 깨진다 |
| dev 서버에서 Postgres 커넥션 고갈 | HMR 마다 모듈이 재평가되어 `new PrismaClient()` 가 쌓인다. `globalThis` 싱글턴(`packages/db/src/client.ts`)이 처방 |
| 🔴 판정 화면에서 대안 기록을 눌러도 같은 🔴 로 돌아옴 | 화면이 `req.input.goal.kind`(사용자가 적어 낸 목표)로 갈렸다. 풀코스 기간 미달이면 엔진이 완주로 바꿔 놓으므로(§7.3) **`plan.feasibility.effectiveGoal.kind`** 를 봐야 한다 |
| GA 이벤트가 안 나가는 것처럼 보임 | `dataLayer` 항목은 배열이 아니라 `arguments` 객체다. `Array.isArray` 로 거르면 전부 사라진다. `Array.prototype.slice.call(e)` 로 본다. 그리고 `TrackEvent` 는 이펙트에서 쏘므로 **탐색이 마운트를 앞지를 수 있다** — 한 번 더 읽어 볼 것 |
| 새 라우트를 만들었는데 `Link href` 가 타입 오류 (`RouteImpl<...>` 에 할당 불가) | `tsconfig.json` 이 `.next/types` 와 `.next-agent/types` 를 **둘 다** include 한다. 한쪽이 오래된 빌드면 옛 라우트 유니온이 이긴다. 안 쓰는 dist 의 `types/` 를 지우고 다시 빌드한다 |
| 프로덕션 빌드를 http 로 띄웠더니 로그인이 안 붙음 | Auth.js 가 `__Secure-` 쿠키를 쓰는데 브라우저가 http 오리진에서 버린다. **서버 로그에는 성공으로 남고 화면만 게스트다.** `AUTH_URL=http://localhost:<포트>` 로 프로토콜을 못 박으면 일반 쿠키를 쓴다 |
| 공개 페이지 본문이 `<main>` 밖에 있음 (JS 끄면 빈 화면) | 라우트 그룹에 `loading.tsx` 가 있으면 Suspense 경계가 생겨 본문이 `<div hidden>` 으로 스트리밍된다. JSON-LD 까지 거기 들어가 **AEO 가 통째로 샌다.** 정적 SEO 페이지가 있는 그룹에는 두지 말고 동적 라우트로 내린다 |
| E2E 에서 로그인 뒤 `goto` 가 `net::ERR_ABORTED` | 서버 액션 리다이렉트가 아직 진행 중인데 다음 이동이 덮어썼다. **성공(이동)과 실패(경고 문구) 중 먼저 오는 쪽**을 기다린다 — 이동만 기다리면 실패 경로에서 타임아웃을 통째로 태운다 |
| `next-env.d.ts` / `tsconfig.json` 이 줄바꿈만 바뀐 채 변경 목록에 뜸 | Windows 개발 서버가 남기는 것. 커밋 전에 `git checkout` 으로 되돌리면 된다 |

### 클라우드 세션(Cowork)에서 작업할 때
사용자 기기의 `node_modules` 심볼릭 링크가 마운트 너머에서 깨져 있어 그 자리에서 빌드가 안 된다.
리포를 기기 홈으로 복사해서 검증한다.

```bash
rm -rf "$HOME/build-check" && mkdir -p "$HOME/build-check"
cd "$HOME/mnt/run-back" && tar -cf - --exclude=node_modules --exclude=.git --exclude=.next . \
  | (cd "$HOME/build-check" && tar -xf -)
# build-check/package.json 에서 "packageManager" 필드를 제거
export PATH="$HOME/pnpmbin/node_modules/.bin:$PATH" COREPACK_ENABLE_STRICT=0
cd "$HOME/build-check" && pnpm install --no-frozen-lockfile && pnpm --filter @runback/web build
```

- 마운트된 폴더에서 `rm` 은 "Operation not permitted" 로 실패한다. `mv` 는 된다.
- 소스 수정은 `$HOME/mnt/run-back` (진짜 리포)에 하고, 검증만 `build-check` 에서 한다. 반대로 하면 작업이 날아간다.

---

## 12. 도그푸딩

작성자 본인이 러너다. 데모 플랜(`lib/demo-plan.ts`)도 실제 목표 대회를 쓴다.

| 대회 | 날짜 | 검증 대상 |
|---|---|---|
| MBN 서울 마라톤 (하프) | 2026-11-15 | 하프 경로 전체 |
| 도쿄 마라톤 (풀) | 2027-03-07 | 풀코스 + 장기 플랜 |

하프까지 10주는 §7.3 최소 권장 주차에 정확히 걸리는 **경계값**이다. 판정 문구와 볼륨 곡선을 실감으로 검증할 기회다.
